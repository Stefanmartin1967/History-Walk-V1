# Changelog Heripia

> Historique des livraisons, extrait de `src/state.js` (commentaire de `APP_VERSION`) le 11/06/2026
> pour alleger le contexte charge a chaque lecture du fichier d'etat (audit A1). Ordre : du plus
> recent au plus ancien. La version courante vit dans `APP_VERSION` (src/state.js).

---

- SCOUT — CATÉGORIES RESYNCHRONISÉES SUR LA TAXONOMIE V2 (v3.7.302) — **PR-1 du chantier « modèle C destination ».** Corrige une classe de bug révélée par l'audit : le mapping de catégories du Scout (`getHwCategory`) était figé sur la taxonomie **pré-v2** → tout candidat moissonné non-mosquée recevait une catégorie **inexistante** (`'Site historique'`, `'Site religieux'`, `'Culture et tradition'`) → icône « ? », et la valeur **perdue silencieusement** au `<select>` du richEditor (option absente). Avec les catégories cochées par défaut (Religion/Histoire/Culture), la **majorité** des lieux d'un premier scan tombaient sur ces 3 libellés cassés. **Fix** : mapping extrait dans un **module feuille** `src/scout-categories.js` (zéro dépendance, testable seul) et resynchronisé sur la taxonomie v2 — museum→`Musée`, fort/castle→`Fortification`, archaeological_site/ruins→`Site archéologique`, cafe→`Café`, church/chapel/cathedral→`Église`, synagogue→`Synagogue`, place_of_worship dérivé de la religion. **Invariant garanti** : toute valeur retournée appartient à `getCategoryLabels()` ; un tag **ambigu** (monument, mémorial, lieu de culte de religion inconnue — app multi-pays) retourne `null` → le candidat est marqué « inconnu » et la capture écrit le sentinel `'A définir'` (badge « à catégoriser » au tri) plutôt qu'une catégorie devinée à tort. Corrige aussi le **sentinel mal orthographié** à la capture (`'À définir'` accentué → `'A définir'` canonique, qui contournait `isPoiMalCategorized`). **Test** : `tests/scout_category_mapping.test.js` croise les sorties de `getHwCategory` avec la taxonomie v2 **réelle** (`public/poi-categories.json`) — verrouille l'invariant + régression anti-libellés cassés. vitest **993 verts** (5 nouveaux), aucune régression. *Vérification : test unitaire croisé avec la taxonomie réelle (le scan complet en preview — moisson Overpass — n'est pas rejouable de façon déterministe).*

---

- DESTINATIONS — HYGIÈNE POST-OPTION A (v3.7.301) — Nettoyage qui clôt le chantier Option A (aucun changement de comportement). (1) **`computePublishCount(stats)`** : helper UNIQUE pour le compteur de publication (`poisModified + circuitsModified + pendingPhotoCount + testedChanged`), qui pilote le badge nav ET le bouton « Tout publier » — la formule était copiée à **3 endroits** dans `admin-control-ui.js` (risque de dérive badge↔bouton). (2) **Branche morte retirée** : depuis l'Option A une destination est soit *publiée* soit *brouillon local* — la notion intermédiaire « brouillon GitHub » n'existe plus. Le label de statut du Scout (`scout.js`) passe de 3 à 2 états (`publiée` / `brouillon local`) ; les commentaires devenus faux (« brouillon GitHub »/« officialiser »/« C2b »/« multi-appareils ») rafraîchis dans `local-destinations.js`, `richEditor.js`, `admin-diff-engine.js`, `map.js`, `utils.js`. (3) Corrigé la description trompeuse de `draftGeoJSON_{id}` (écrit **vide** à la création — les lieux scoutés vivent dans `customPois_{id}`). **Non touché** (volontaire) : la garde géo Scout reste **fail-open** (en Option A un brouillon a toujours des bounds valides ; passer en fail-closed risquerait de faux blocages pour un cas qui ne se produit pas). vitest **988 verts**, build Vite OK. knip : non exécutable localement (OOM oxc-parser du sandbox) — vérifié par la CI Qualité de la PR ; ce diff ne touche aucun export/import/fichier/dépendance, donc aucun finding possible. Preview : 7 modules transformés OK + boot admin propre (refacto pur du compteur, couvert par les tests).

---

- DESTINATIONS — LA SAUVEGARDE PROTÈGE LE BROUILLON LOCAL, OPTION A (v3.7.300) — En Option A un brouillon vit **uniquement en local** (IndexedDB) → un plantage DB le perdrait. La sauvegarde l'embarque désormais. **Constat** : le backup contenait déjà les lieux (`baseGeoJSON`) + la curation (`userData`), mais PAS la **définition** de la destination (`draftDestinations[id]`) ni les **quartiers** (`draftZones`) ; et à la restauration les captures Scout (id `HW-ULID`) étaient **ignorées** par le filtre des POIs custom (`HW-PC-`/`auto_`). **Export** (`fileManager.js` — `saveUserData` + `prepareExportData`, via le helper `attachLocalDraft`) : si la dest active est un brouillon local (`custom:true`), joint `draftDestination` (l'entrée) + `draftZones` à **toutes** les sauvegardes (Légère ET Complète — restauration toujours complète, décision Stefan). No-op sur une dest publiée. **Restore** (`restoreBackup`) : si `draftDestination` présent → recrée le brouillon (`createLocalDraftDestination` : entrée + zones + geojson base vide), restaure **TOUS** ses lieux en `customPois` (plus seulement `HW-PC-`/`auto_`), et bascule `hw_active_dest` dessus → au reload le brouillon réapparaît complet. **Rétro-compatible** : champs optionnels ; anciens backups et dests publiées restaurés comme avant. vitest **988 verts** (9 nouveaux tests export/restore brouillon), knip 0, build Vite OK. **Vérifié en preview** : `prepareExportData` sur un brouillon simulé inclut `draftDestination`+`draftZones` ; absents sur une dest publiée (la restauration réelle, qui écrit l'IDB + reload, est couverte par les tests unitaires).

---

- DESTINATIONS — « SUPPRIMER UNE DESTINATION » (BROUILLON LOCAL), OPTION A (v3.7.299) — Ajoute la fonction manquante de **suppression d'un brouillon local** (remplace le snippet console). Périmètre volontairement limité aux **brouillons locaux** (`custom:true`) : après l'Option A c'est le seul cas existant (data 100% locale, pas de token, pas de GitHub) ; la suppression d'une destination **publiée** reste hors scope (décision Stefan). **Carte CC › Outils** « Supprimer cette destination » (style danger, icône `trash-2`), visible **uniquement sur un brouillon local**, en dernier (action destructive séparée). Confirmation `hwConfirm` explicite (irréversible, suggère une Sauvegarde préalable) puis purge **100% locale** via **`deleteLocalDraftDestination(id)`** (`local-destinations.js`) : entrée retirée de `draftDestinations`, clés appState `draftGeoJSON_`/`draftZones_`/`customPois_`/`lastZones_`/`lastZones_etag_` supprimées, **toutes les données IDB de la carte** effacées via le nouveau **`deleteAllMapData(mapId)`** (`database.js` — une transaction multi-stores sur `poiUserData`/`savedCircuits`/`poiPhotos`/`osmNearestWay` via leur `mapId_index`), boîte de scan `scout_box_{id}` retirée, et reflet mémoire (`state.destinations.maps[id]` supprimé). Puis bascule sur la dest publiée par défaut + reload → état propre. **Garde** : la carte n'apparaît que sur `custom:true` → impossible de supprimer Djerba/une dest publiée par ce chemin. vitest **982 verts** (6 nouveaux tests `deleteLocalDraftDestination`), knip 0, build Vite OK. **Vérifié en preview** : carte présente sur un brouillon local / absente sur Djerba ; suppression → purge effective + bascule Djerba.

---

- DESTINATIONS — MODÈLE 2-PHASES (BROUILLON LOCAL → PUBLIÉ), OPTION A (v3.7.298) — Refonte du cycle de vie d'une destination pour le rendre **propre et privé**. **Constat fondateur** (audit) : le dépôt étant **public**, un « brouillon » poussé sur GitHub (auto-enregistrement #852) **n'était jamais confidentiel** — sa data était téléchargeable en raw anonyme ; le statut `draft` ne masquait que l'affichage. Le cycle gérait en plus **3 représentations** (IndexedDB local / entrée GitHub `draft` / mémoire) et **le mot « brouillon » à 3 sens**, pour absorber une latence Pages de 2 min. **Nouveau modèle (2 phases)** : (1) **Édition** = le brouillon vit **uniquement en local** (IndexedDB, `custom:true`), **rien sur GitHub**, admin-only par construction → *vraiment* privé, et **création sans token requis** (plus aucune écriture distante) ; (2) **Publiée** = « **Publier cette destination** » (Outils) effectue le **1er et seul push** en `status:"published"` (visible de tous). Suppressions : `registerDraftDestinationOnGitHub` (push à la création) retiré de `scout.js` ; `publishDraftToGitHub` + `officializeDestination` **fusionnés** en **`publishDestination(id)`** (`publish-destination.js`) — pousse les 4 fichiers (données d'abord, `destinations.json` en dernier), met de côté les **candidats non curés** en local (`keepCandidates:false`, garde-fou inchangé), refuse s'il n'y a **aucun lieu curé**. UI : les 2 cartes (« Publier en brouillon GitHub » + « Officialiser ») **fusionnées en une** (`admin-control-ui.js`). **Garde anti-orphelin** : « Tout publier » est **bloqué sur un brouillon local** (sinon geojson sans entrée `destinations.json` = piège historique Hammamet) et **redirige** vers « Publier cette destination » (`admin-control-center.js`). vitest **976 verts** (13 tests `publishDestination` réécrits), knip 0, build Vite OK. **Vérifié dans l'app réelle** : boot propre 0 erreur ; CC › Outils — carte « Publier cette destination » **absente** sur une dest publiée, **présente** (libellé « Brouillon local → publiée ») sur un brouillon local. La création/publication réelle (effets GitHub) non déclenchée en preview — couverte par les tests unitaires.

---

- SCOUT — BOÎTE DE SCAN PAR DÉFAUT + MÉMORISÉE PAR DESTINATION (v3.7.297) — Le bouton « Scanner la zone » était **grisé à l'ouverture** du Scout tant qu'on n'avait pas tracé une boîte à la main (condition `!_box`) → impression de bug, surtout pour un **re-scan** (enrichissement data : nouvelles catégories, lieux récemment cartographiés). Désormais Scout **pose une boîte d'emblée** : la « **boîte de la destination** » mémorisée (dernier scan, `localStorage` `scout_box_{mapId}`) si elle existe et touche encore la destination, **sinon une boîte par défaut** centrée sur la vue (~60 % du conteneur carte) → « Scanner » **actif tout de suite**, ajustable aux **poignées** (agrandir la zone sans rien perdre — le scan **dédoublonne** l'existant, règle #472) ou « Retracer » pour une autre zone. La boîte est **persistée** au scan (« le 1ᵉʳ scan crée la boîte »), au redimensionnement/déplacement (`onPointerUp`) et au tracé (`onDrawUp`) ; « Retracer » (`clearBox`) l'oublie. Préférence **locale par appareil** (comme `hw_active_dest`). `scout.js` : helpers `boxStorageKey`/`persistBox`/`loadSavedBox`/`defaultBox` + restauration dans `renderShell`. Aucun changement CSS. build Vite OK, vitest 982, knip 0. **Vérifié dans l'app réelle** : ouverture → « Scanner » actif + boîte 60 % posée ; boîte sauvée → restaurée (≠ défaut) ; scan → boîte persistée en `localStorage`.

---

- FIX CLASSE DE BUG — LECTEURS BRUTS vs OVERLAY (suite audit) (v3.7.296) — Corrige les **6 dernières instances** (audit adversarial) où un lecteur d'AFFICHAGE/COMPTAGE lisait `feature.properties.X` **brut** en ignorant l'overlay `userData` → une curation admin (recat / dé-vérification / ajout de photo par URL) ne s'y reflétait pas. Même classe que les fixes catégories (`effectiveCategory`) et Zone (#857 puis dégel #858). Tous remplacés par **`getPoiProp(feature, 'X')`** (overlay prioritaire, déjà testé) : **`mode-donnees.js`** (liste admin) — catégorie (l'icône overlay-aware contredisait le libellé brut) ET **badge « Vérifié »** (l'ancien `props.verified || userData?.verified` court-circuitait : un override `false` sur une base `true` ne retirait JAMAIS le badge — `getPoiProp` gère le `false`) ; **`mobile-nav.js`** (méta « zone · catégorie » des résultats de recherche) ; **`circuit.js`** + **`mobile-poi.js`** (hero/compteur photos de la cover de circuit, PC + mobile) ; **`mobile-follow.js`** (vignette photo de l'aperçu du mode Suivi + son déclencheur d'hydratation IDB). Aucun changement CSS. vitest **982 verts** (mocks `getPoiProp` adaptés), build Vite OK, knip 0. **Vérifié dans le rendu réel** (Mode Données, DOM) : un POI base `verified:true` + overlay `verified:false` → badge **masqué** ; catégorie overlay affichée. Clôt la classe de bug « lecteurs bruts vs overlay ».

---

- DÉGEL DE ZONE — DÉRIVÉE À LA VOLÉE (clôt le chantier re-zonage) (v3.7.295) — La `Zone` d'un POI n'est plus une valeur GELÉE à la capture : elle se **DÉRIVE à la volée** de la position + du jeu de quartiers courant (`getDerivedZone`, comme `planifieCounter` l'avait été pour tuer les désynchros). **Effet** : compléter/pousser le fichier `{dest}-zones.geojson` re-zone tout le monde (admin ET visiteur) **automatiquement** au prochain chargement — fini le « Recalculer » + « Tout publier » des zones. **Tue aussi une classe de bug** (lecteurs de `Zone` figée incohérents avec le jeu de quartiers courant). **Helper** `getDerivedZone(feature)` (utils.js) : dérive quand les quartiers sont chargés, sinon **repli sur la valeur stockée** (overlay-aware) — JAMAIS « Hors zone » fabriqué si le fetch des zones échoue (corrige aussi le **bug critique** : `getZoneFromCoords` renvoyait « Hors zone » pour TOUS sur un `features:[]` truthy → durci en `.length === 0` → « A définir »). Mémoïsé par id (cache dans `zones.js`, vidé par `setZonesData` au changement de jeu, par `deleteZoneCacheEntry` au déplacement d'un POI). **Branché** sur les **6** lecteurs de la Zone stockée (l'audit en avait raté 2) : filtre topbar (`passesStructuralFilters`), compteur du panneau (`getZonesData`), fiche PC (`templates.js`), index des circuits poussé sur GitHub (`buildCircuitIndexEntry`), et **cuisson au publish** (`generateMasterGeoJSONData` re-cuit une Zone fraîche dans le geojson public via `deriveZoneSafe`, gardée contre les zones vides — filet robustesse pour un visiteur dont le fetch échoue). **Arrêt des écritures qui figeaient** `Zone` : capture Scout (`scout.js`), déplacement de POI (`data.js`), save du richEditor (`richEditor.js` — l'input reste affiché en lecture seule), ajout de POI mobile (`mobile-nav.js` — supprime au passage son point-dans-polygone inline bugué qui ne gérait que Polygon). **Mobile/circuits : zéro changement** (ils dérivaient déjà via `getZoneFromCoords`). **`recalc-zones.js` allégé** : la boucle de ré-affectation des POIs (PR C #856/#857) est **retirée** (devenue inutile) ; ne reste que la **complétion du fichier de quartiers** (fetch OSM bbox réelle + merge + push) → carte CC renommée **« Compléter les quartiers »**. Tests : `getDerivedZone`/`deriveZoneSafe` (dérivation, ignore le stocké quand zones chargées, filet, invalidation cache, garde zones-vides), `completeDestinationZones` réécrit, `data_module`/`circuit_actions_zones`/`admin_geojson`/`circuit-index-entry` adaptés. **vitest 982 verts**, build Vite OK, knip 0. **Hammamet** : « Compléter les quartiers » (si pas déjà complet) → recharger → les 36 retrouvent leur quartier **tout seuls**, sans « Tout publier ». *(Absorbe et clôt la PR C / #857 : « propre > rapide ».)*

---

- FIX COMPTEUR DE ZONES — LIT L'OVERLAY (suivi PR C re-zonage) (v3.7.294) — Après « Recalculer les zones » (#856), Hammamet montrait toujours **36 « Hors zone »** dans le filtre alors que les 36 POIs étaient bien re-zonés (vérifié : les 59 quartiers OSM résolvent les 36 ; carte/fiche correctes). Cause : `getZonesData()` (`circuit-actions.js`, source de la liste de comptage du filtre Quartier) lisait `feature.properties.Zone` **brut**, en IGNORANT l'overlay `userData` — alors que le recalcul écrit la nouvelle `Zone` des POIs de base dans l'overlay `userData.Zone`. Incohérence : le **filtrage** par zone, lui, est déjà overlay-aware (`passesStructuralFilters` merge `{ ...properties, ...userData }`) → cliquer « Hors zone » n'affichait aucun des 36 (rangés ailleurs par l'overlay) mais le **compteur** en affichait 36. Bug **préexistant** (même nature que celui déjà corrigé pour les catégories via `effectiveCategory`), révélé par PR C, 1ʳᵉ feature à changer `Zone` en masse via overlay. Fix : `getZonesData` lit via **`getPoiProp(feature, 'Zone')`** (overlay prioritaire). +1 test régression (`circuit_actions_zones.test.js` : `Zone='Hors zone'` + `userData.Zone='Neapolis'` → compte Neapolis). Vérifié dans l'app réelle (Hammamet : overlay sur 3 POIs → « Hors zone » 36→33, Neapolis 10→12, Beni Khiar +1). **RAPPEL** : « Tout publier » ne tourne JAMAIS automatiquement — pour figer les 36 dans `hammamet.geojson` (et les rendre publics), faire CC → **Tout publier** (le recalcul n'a poussé que le fichier zones). Aucun changement CSS. vitest 977 verts.

---

- RECALCULER LES ZONES (PR C du chantier re-zonage) (v3.7.293) — Répare une destination DÉJÀ scoutée dont des POIs sont « Hors zone » (cas Hammamet : 36/82), SANS la re-scouter (curation préservée). Cause : à la création, les zones OSM n'étaient récupérées que pour la bbox (serrée) de la destination, alors que les POIs débordaient bien au-delà (jusqu'à Korba/Zriba) → hors du jeu de quartiers. Nouvelle carte CC **« Recalculer les zones »** (`btn-cc-tool-recalc-zones`, icône `layers`, après la carte Scout) → nouveau module **`recalc-zones.js`** `recalcActiveDestinationZones()` : bbox = **étendue RÉELLE des POIs chargés** (pas `dest.bounds`, +pad 0.01°) → `fetchZonesAuto(bbox, dest.country)` → **MERGE** dédupliqué par `osm_id` dans le binding live `zonesData` (`setZonesData`, miroir du Scout : on ne perd jamais une zone connue). Puis ré-affecte la `Zone` de chaque POI **uniquement si elle change** (idempotent : un POI déjà bien zoné n'est pas touché → pas de faux diff) — POI de base → overlay `userData`+`savePoiData` ; POI custom → `properties`+`customPois_{id}` (logique duale d'`executeEdit`, évite le bug overlay) ; 1 event `admin:poi-edited` par POI re-zoné, **1 seul `applyFilters()`**. Persistance des zones : brouillon LOCAL → `saveDraftZones` (IndexedDB) ; brouillon GitHub / dest publiée → nouvelle fonction **`pushDestinationZones(id, zones)`** (`publish-destination.js`) qui pousse `{id}-zones.geojson` (« Tout publier » ne pousse JAMAIS les zones, d'où le push dédié ; token requis). **Nuance** : la `Zone` affichée d'un POI est sa propriété STOCKÉE (figée à la capture), pas re-dérivée à l'affichage → ce sont la ré-affectation + le **« Tout publier »** (qui cuit la `Zone` dans le geojson public) qui réparent les 36 sur tous les appareils ; le push du fichier zones garde le jeu de quartiers complet pour les futures captures/recalculs. La modale de succès le dit explicitement. Dépend de PR A (MultiPolygon) + PR B (`country` sur `destinations.json`). Aucun changement CSS. *(Chantier re-zonage : A robustesse, B Scout, C recalcul — COMPLET.)*

---

- SCOUT — ÉTEND LES ZONES OSM À LA BOÎTE DE CAPTURE (PR B du chantier re-zonage) (v3.7.292) — Cause des « Hors zone » de Hammamet : les zones n'étaient récupérées que pour la bbox (serrée) de la destination à la création, alors que le Scout capture souvent bien au-delà → les POIs hors de cette bbox tombaient « Hors zone ». Désormais, au DÉBUT de `capture()` (`scout.js`), on **fetch + merge les zones OSM de la boîte de capture** (`_box`) AVANT d'assigner `Zone` (try/catch séparé NON bloquant : un échec Overpass n'empêche pas la capture). Merge dédupliqué par `osm_id` dans le binding live `zonesData` (`setZonesData`) ; persistance `draftZones_{id}` pour un brouillon LOCAL (`saveDraftZones`, nouveau dans `local-destinations.js`). Prérequis : **champ `country`** ajouté aux entrées `destinations.json` (le Scout le stocke via `_geoCountry` à la création ; propagé par `registerDraftDestinationOnGitHub`/`publishDraftToGitHub` ; backfill `tn` sur Djerba + Hammamet) — sinon `fetchZonesAuto` ne choisit pas le bon niveau admin (TN = délégations niveau 6). Couvre les futures captures ; le Hammamet DÉJÀ scouté (36 « Hors zone ») sera réparé en place par la **PR C** (« Recalculer les zones » + push du fichier de zones). Dépend de la PR A (MultiPolygon) pour les délégations morcelées. Aucun changement CSS. vitest verts (972).

---

- FIX ROBUSTESSE ZONES — MULTIPOLYGON (PR A du chantier re-zonage) (v3.7.291) — Deux corrections appariées sur l'affectation de la `Zone` d'un POI, sans toucher au comportement Polygon actuel. **(1) `getZoneFromCoords` (`utils.js`)** lisait `feature.geometry.coordinates[0]` inconditionnellement → correct pour un `Polygon` (anneau extérieur) mais **cassé pour un `MultiPolygon`** (`coordinates[0]` = un sous-polygone entier, pas un anneau de points) → `isPointInPolygon` recevait une structure malformée → la zone ne matchait jamais → tous ses POIs tombaient « Hors zone ». Désormais : branche sur `geom.type` (Polygon → `coordinates[0]` ; MultiPolygon → on teste l'anneau extérieur `poly[0]` de chaque sous-polygone) + gardes défensives (feature sans géométrie). **(2) `osm-zones.js` `fetchZonesForBBox`** ne gardait que le **plus grand anneau** d'une relation admin multi-parties (`rings.sort` + `coordinates:[rings[0]]`) → un POI dans une partie secondaire = « Hors zone ». Désormais : un seul anneau → `Polygon` ; plusieurs → `MultiPolygon` (tous les anneaux conservés). Les deux fixes sont appariés (osm-zones émet du MultiPolygon, getZoneFromCoords le gère). Les `*-zones.geojson` publiés actuels (Djerba) sont tous Polygon mono-anneau → chemin inchangé, zéro régression. Tests : `tests/utils.test.js` (+3 : Polygon, point dans le 2ᵉ sous-polygone d'un MultiPolygon = régression du bug, feature sans géométrie). Aucun changement CSS. vitest verts (972). *(Prochaines PR du chantier : B = Scout étend les zones à la bbox de capture ; C = carte CC « Recalculer les zones » + push du fichier de zones — répare le Hammamet actuel.)*

---

- FIX CURATION SCOUT — ÉDITION D'UN POI CAPTURÉ + GRADUATION À LA PUBLICATION (v3.7.290) — Corrige le bug racine « la curation d'un POI du Scout disparaît après publication » (+ le badge bloqué). **Volet 1 (`richEditor.js` `executeEdit`)** : éditer un POI CUSTOM (capturé Scout / créé local) écrivait les champs dans l'overlay `userData` — comme un POI de base. Mais un POI custom n'a pas de patrimoine à surcoucher ; au « Tout publier » l'overlay est cuit dans le geojson GitHub PUIS supprimé, et la version BRUTE restée dans `customPois_{id}` re-masquait la version curée au reload (fusion « custom gagne ») + le diff voulait revenir en arrière. Désormais une branche `isCustom` (détection canonique `state.customFeatures.some(getPoiId===id)`) écrit les champs **dans `feature.properties` + `customPois_{id}`** (parité `executeCreate`, purge des clés vidées, purge d'un overlay résiduel) ; le POI de base garde l'overlay `userData` **inchangé**. **Volet 2 (`admin-control-center.js` `publishChanges`)** : après publication, les POIs custom publiés (`diffData.pois`) sont **sortis de `customFeatures` + `customPois_{id}`** (graduation vers le geojson de base) — sinon ils re-masqueraient la base et `reconcileLocalChanges` les re-marquerait `'creation'` à chaque boot (badge fantôme permanent). Les candidats non curés (hors `diffData.pois`) restent locaux. Explique aussi « clic-droit OK / scout KO » (création écrit déjà dans properties). **Réconciliation 1-fois** des brouillons existants (entrées brutes masquant une base déjà curée) = purge guardée de `customPois_{id}` (snippet admin). Note : transitoire de déploiement Pages ~1-2 min possible après une 1ʳᵉ publication. Aucun changement CSS. vitest verts (969), esbuild OK, knip 0.

---

- GARDE-FOU FLUX DESTINATION — AUTO-ENREGISTREMENT À LA CRÉATION + BLOCAGE SANS TOKEN (v3.7.289) — Tue l'état « brouillon LOCAL seul » qui a causé les geojson orphelins / curation perdue (cas Hammamet). Créer une destination via le Scout l'**enregistre immédiatement sur GitHub** en `status:"draft"` : nouvelle fonction `registerDraftDestinationOnGitHub(id, entry)` (`publish-destination.js`), **id-keyée** et SANS les gardes `state.currentMapId` / `loadedFeatures` / « 0 lieu » de `publishDraftToGitHub` (un brouillon neuf a un geojson VIDE, c'est voulu — on le remplit ensuite par Scout + « Tout publier »). Pousse, données d'abord et destinations.json EN DERNIER (invariant anti-orphelin) : `{id}.geojson` (vide) + `{id}-zones.geojson` + `circuits/{id}.json` + `destinations.json`. Appelée à la fin de `createDestinationDraft` (`scout.js`), **awaitée AVANT le reload** (sinon la navigation interrompt le push), puis `markLocalDraftPublished`. **Blocage si pas de token** : `createDestinationDraft` refuse (toast) AVANT toute écriture IndexedDB si `!getStoredToken()`. Échec réseau → repli sur brouillon local + avertissement (carte « Publier en brouillon GitHub » du CC pour retenter). 1ère pièce du chantier garde-fous (suite : blocage « Tout publier » sur état incohérent, action « Supprimer une destination »). Aucun changement CSS. vitest verts (969), esbuild OK.

---

- DESIGN / CARTEL — MINI-CARTELS : coins adoucis (suivi #850) (v3.7.288) — Retour Stefan : les étapes rendaient « carré ». Le handoff avait redescendu le rayon de la carte d'étape à `--radius-sm` (6px) ; la démo validée (et l'ancienne timeline) étaient à **14px**. Correction : `.cartel-step` `border-radius` → **`--radius-lg`** (14px) + bordure de la pastille numéro allégée (`--border-filet-strong` 2px → `--border-filet` 1px) pour retrouver la douceur de la démo. CSS pur, `cartel.css`. `index.html` : `style.css?v=8.18`. Vérifié en preview PC + mobile. vitest verts, knip 0.

---

- DESIGN / CARTEL — TIMELINE EN MINI-CARTELS (étapes de circuit) (v3.7.287) — Dernière pièce du lot Cartel : chaque étape d'un circuit devient un **mini-cartel** (handoff Claude Design `ui_kits/heripia-fiche`, conforme au brief validé). **Nom en serif** (`--font-serif`, héros), **catégorie en eyebrow petites-capitales sépia affichée SEULEMENT si absente du nom** — nouveau `isCategoryInName(feature, label)` (`data.js`, normalise casse+accents via `\p{Diacritic}`, sous-chaîne sur **toutes** les variantes de nom FR+arabe via `getSearchableNames`) → un circuit de mosquées n'écrit plus « Mosquée » sur chaque « Mosquée X ». **Pastille numéro** = ordre + état : `is-done` (lieu visité `vu`) → pastille **olive cochée** (nom NON barré — décision Stefan, on ne raye pas un monument) ; `is-current` → liseré terracotta + anneau (**CSS prêt, réservé au suivi GPS**, pas câblé en consultation). Composant partagé **`.cartel-step`** dans `style/cartel.css` (piloté tokens, 4 moments) ; PC (`circuit-view.js` `createStepElement` + `panels.css`) et **mobile** (`mobile-poi.js` timeline + liste Lieux + `mobile.css`, chevron conservé) sur les **mêmes classes** (parité). **remplacer = nettoyer** : anciens `.timeline-step`/`.step-*` (panels.css) + `.cc-step*` (mobile.css) **+ ancienne timeline morte `.cc-list`/`.cc-step` pré-V2** (panels.css, ~134 lignes, conteneur rendu nulle part) supprimés. **Reporté** (migration séparée, diverge de #849) : le bandeau de stats `.cartel-stats` du header circuit. `index.html` : `style.css?v=8.17`. PC + mobile, vérifié en preview ; vitest verts, knip 0.

---

- DESIGN / CARTEL — VOLET CIRCUIT : « POI » → « LIEUX » + RETRAIT DU SÉPARATEUR « ÉTAPES » (v3.7.286) — Consolidation du panneau circuit (retour Stefan). (1) **Compteur unique** : le nombre de lieux s'affichait DEUX fois — dans l'eyebrow du cartel (`zone · N · km`) ET dans l'en-tête « Étapes · N POIS » de la liste. L'**en-tête « Étapes » est retiré** (PC `.circuit-timeline-cap` + mobile `.cc-timeline-cap`, JS + CSS — remplacer = nettoyer) : la liste numérotée se suffit, le compteur ne vit plus que dans l'eyebrow. « Étapes » évoquait d'ailleurs un trek multi-jours pour une balade de 2 h. (2) **« POI » → « lieux »** partout en visible (jargon retiré) : eyebrow panneau (`N POIS → N lieux`), cartes circuit PC (`ui-circuit-list` va-stat) + mobile (`mobile-circuits` mc-stat, `mobile-poi` eyebrow), état vide (« Aucun lieu dans ce circuit »), corbeille (`circuit-trash-ui`). L'**aide circuit** (`help-content`) n'explique plus l'abréviation « POI » (devenue fausse à l'écran). L'**admin garde « POI »** (interne : OSM, photo-batch, diff). Drag Sortable simplifié (le décalage d'index lié au cap n'a plus lieu d'être). `index.html` : `style.css?v=8.16`. PC + mobile, vérifié en preview ; vitest verts, knip 0. Suite du lot Cartel : mini-cartels des étapes (via brief Claude Design).

---

- DESIGN / CARTEL — i18n-1b : RECHERCHE AGNOSTIQUE + TRI & POINT DE DÉPART DANS LA LANGUE (v3.7.285) — 3ᵉ et dernière PR du chantier i18n « noms patrimoniaux » (volet logique). **Recherche AGNOSTIQUE à la langue** : nouveau `getSearchableNames(feature)` (data.js) renvoie TOUTES les variantes de nom (custom + FR + arabe + AR + brut) ; le matching de `getSearchResults` (search.js) + des recherches circuit (`mobile-circuits.js`, `ui-circuit-list.js`) porte dessus → **on retrouve un lieu en tapant son nom FR OU arabe quel que soit le réglage** (chercher ≠ afficher — corrige le « je tape en arabe, 0 résultat » de Stefan). Les **étiquettes** de résultats suivent la langue affichée (`searchManager.js`, `mobile-nav.js` → `getPatrimonialName`). **Tri dans la langue affichée** : `getSearchResults` trie par `getPatrimonialName` avec la collation locale (`ar` en arabe, `fr` sinon) → l'ordre suit ce que l'utilisateur lit. **Point de départ qui suit la langue** : `buildHomeFromFeature` stocke désormais le `poiId` ; `getStartPointLabel` résout le nom à la volée via `getPatrimonialName` (repli sur le label figé pour le GPS « Ma position » ou un POI disparu) → « Depuis X » bascule FR/AR (avec `dir="auto"`). **Nettoyage** : 5 imports `getPoiName` morts retirés (circuit-flags, desktopMode + restes i18n-0 : templates, ui-details, ui). **Tests** : `tests/patrimonial_names.test.js` (getPatrimonialName repli/custom, getSearchableNames, recherche agnostique FR↔AR + POIs cachés) + `start_point.test.js` étendu (poiId + résolution langue). **RESTENT en FR (volontaire)** : noms de circuits officiels, GPX, admin. Aucun changement CSS. vitest verts, knip 0.

---

- DESIGN / CARTEL — i18n-1a-bis : `dir="auto"` SUR LES NOMS PATRIMONIAUX EN LISTE (alignement bidi) (v3.7.284) — Polish de i18n-1a (retour Stefan). Les noms de POI affichés en arabe dans les LISTES portaient le bon texte mais sans direction explicite (contrairement au titre de fiche déjà en `dir="rtl"`). Ajout de **`dir="auto"`** sur les éléments de nom : étape de timeline circuit (`.step-name`, `circuit-view.js`), étapes mobile (`.cc-step-name`, `mobile-poi.js` ×2), viewer d'étape suivi mobile (`.pv-name`, `mobile-follow.js`), et les chips « Filtré par X » (`<strong>` dans `ui-circuit-list.js` + `mobile-circuits.js`). `dir="auto"` = direction déduite du 1er caractère fort → **arabe RTL** (aligné à droite), **français LTR** (aligné à gauche), automatiquement par élément. **Aucun changement CSS, aucune logique.** Vérifié en preview PC (timeline AR alignée à droite, FR à gauche). vitest verts, knip 0.

---

- DESIGN / CARTEL — i18n-1a : BALAYAGE D'AFFICHAGE DES NOMS PATRIMONIAUX (visiteur) (v3.7.283) — 2ᵉ PR du chantier i18n. i18n-0 avait basculé la seule FICHE ; ici **tout ce qu'un VISITEUR voit suit la langue** (`getPoiName` → `getPatrimonialName`), corrigeant le saut « liste FR → fiche AR » signalé par Stefan. Surfaces basculées : **timeline d'étapes de circuit** (`circuit-view.js` — la liste POI d'un circuit), **tooltip de marqueur carte** (`map.js`), **listes & chips POI** (`ui-circuit-list.js` explorer + chip « Filtré par X » + empty state ; `mobile-poi.js` liste « Lieux » + cartes circuit ; `mobile-circuits.js` chip), **suivi de circuit mobile** (`mobile-follow.js` marqueurs + viewer d'étape), **dialogue corbeille** (`ui-modals.js`), **titre de la grille photos** (`ui-photo-grid.js`). **Re-render LIVE au changement de langue** : la fiche se re-rend via `ui-details` (i18n-0) ; `applyFilters()` couvre marqueurs (PC) + listes qui écoutent `data:filtered` (explorer PC, « Lieux » mobile) ; le **panneau circuit/timeline** — qui n'écoute PAS `data:filtered` — est re-rendu explicitement par un coordinateur `patrimony:lang-changed` dans `events-bus.js` (PC `renderCircuitPanel` ; mobile via events `mobile:render-circuits-list`/`mobile:render-poi-list`). **RESTENT en FR (volontaire)** : noms de circuits officiels (`generateCircuitName`), export GPX, et tout l'**ADMIN** (Scout/OSM/photo-batch/mode-données/diff/access-point). **RESTE i18n-1b** : recherche AGNOSTIQUE (matche FR+AR, étiquette langue active — `searchManager`/`search.js`/`mobile-nav` + matches `mobile-circuits:142`/`ui-circuit-list:625`) ; tri dans la langue active ; point de départ → `poiId` (`start-point.js`) ; tests `getPatrimonialName`. **Aucun changement CSS** (`style.css` inchangé). PC + mobile, vérifié en preview ; vitest verts, knip 0.

---

- DESIGN / CARTEL — i18n-0 : BASCULE GLOBALE DE LANGUE DES NOMS (FR ⇄ AR) — FONDATION + FICHE (v3.7.282) — 1ʳᵉ PR du chantier i18n « noms patrimoniaux ». Décidé avec Stefan après analyse data (**85 % des POIs ont un vrai nom arabe**, 92-100 % du cœur patrimonial). **Nouveau module `src/patrimonial-names.js`** : préférence persistée de langue des noms (calquée sur le moment/`theme.js` — localStorage `hw_patrimony_lang` + `saveAppState` + attribut `data-names-lang`) MAIS qui **émet un event `patrimony:lang-changed`** (le moment, lui, est du CSS pur à repeint gratuit ; changer de langue exige de RE-RENDRE les noms déjà affichés). **`getPatrimonialName(feature, lang)` (`src/data.js`)** : en AR renvoie le nom arabe du lieu s'il existe (repli FR sinon ; `custom_title` prime). **Réglage GLOBAL de langue, deux entrées selon la plateforme** : segmenté `.names-toggle` dans le topbar (PC, à côté du moment — « lumière + langue » ; `setupNamesToggle` topbar-v2.js ; CSS jumeau de `.hw-moment-toggle`) **+ entrée « Langue des noms » dans le menu mobile** (`mob-action-names`, jumelle du « moment » `cycleTheme` ; icône `languages` ; sous-titre « Actuel : Français/Arabe »). **PAS de bascule par fiche** : le titre suit la langue (`dir="rtl"` en AR) et **le sous-titre arabe permanent est RETIRÉ** (`.cartel-title-ar`, templates.js + CSS cartel/panels — remplacer = nettoyer) ; la fiche ouverte se **re-rend** au changement de langue (`ui-details` écoute l'event) ; le « Rechercher sur Google » suit aussi la langue. **La pilule `.cartel-namebtn` envisagée en PR-B est ABANDONNÉE** (retour Stefan : redondante avec le réglage global — un réglage global vit dans le chrome PC/mobile, pas dupliqué sur chaque fiche). **Restauration au boot** (`main.js`). `index.html` : `style.css?v=8.15`. **Périmètre i18n-0 = la FICHE** : les ~22 autres surfaces (recherche, listes, étapes, marqueurs) basculeront en **i18n-1** (+ recherche AGNOSTIQUE — matche FR ET AR quel que soit le réglage, car chercher ≠ afficher ; tri dans la langue active ; point de départ qui suit la langue ; tests). PC + mobile. vitest verts, knip 0.

---

- DESIGN / CARTEL — HEADER v3 (2/3) : RANGÉE UTILITAIRE + AIDE « ? » DÉ-EMPILÉES DU TITRE (v3.7.280) — 2ᵉ des 3 PRs « header de fiche ». Avant : la nav ‹3/4›, le « ? » d'aide et le titre s'empilaient au même endroit (retour Stefan : surchargé, incohérent — trois choses sans rapport). Maintenant une **rangée utilitaire `.cartel-utility`** : eyebrow (`zone · catégorie`) à gauche ; à droite, dans `.cartel-utility-actions`, les affordances qui ne concurrencent plus le titre — la **nav circuit** `.cartel-nav` (sortie de l'eyebrow, plus de `margin-left:auto`) et l'**aide « ? »** `.cartel-help` (puce papier à filet, icône `circle-help`). L'aide est **câblée au patron partagé** : `setupHelpButton` (ui-details.js) ne bricole plus un wrapper flex inline autour du titre (hack inline-style supprimé) — il fait juste `attachHelp(.cartel-help, GUIDE_LIRE_LIEU)`. Import `helpButton` devenu inutile retiré. Le **titre est désormais seul** sur sa ligne (dé-empilé). PC + mobile (overrides compacts du header sticky dans panels.css). CSS dans `style/cartel.css`, piloté tokens (aucun littéral rayon/ombre/police). `index.html` : `style.css?v=8.14`. La **bascule de nom `.cartel-namebtn`** N'EST PAS posée ici (pas d'affordance morte) : elle arrive au **chantier i18n** avec le câblage de la bascule globale de langue des noms. Le sous-titre arabe permanent reste jusque-là. Suite : chantier i18n (3/3 conceptuel).

---

- DESIGN / CARTEL — HEADER v3 (1/3) : COMPTEUR ARRONDI + STATUT DESCENDU DE LA PHOTO (v3.7.279) — 1ʳᵉ des 3 PRs « header de fiche » du handoff Design v3. (1) **Le statut quitte la photo** (cohérence avec l'Option A : plus aucune info posée sur une image). Les anciennes épingles État/Accès/Incontournable/« À évaluer » (puces papier en bas du hero) deviennent une **ligne d'état `.cartel-status`** nette SOUS le titre, dans le cartel (encre sur papier, pastille + icône sémantiques, séparateurs en filet). Le hero ne porte plus QUE la photo + le compteur (et la plaque « Ajouter une photo » à vide). `buildHero` ne reçoit plus `tagsHtml` ; `.poi-hero-overlay`/`.poi-hero-tags` retirés (templates.js + panels.css). (2) **Compteur de photos arrondi + unifié** : il devient une puce papier `.cartel-chip` (rayon `--radius-md`, coins doux) positionnée par `.cartel-hero-count` — la **fiche POI ET les covers de circuit** (PC `circuit.js` + mobile `mobile-poi.js`) consomment la même grammaire ; anciens `.poi-photo-count`/`.cp-photo-count`/`.cc-photo-count` (3 blocs identiques) supprimés au profit d'un seul composant (remplacer = nettoyer, net négatif de lignes). CSS dans `style/cartel.css` (piloté tokens : `--radius-md`, `--ok-fg`/`--warn-fg`/`--danger-fg`/`--brand`, `--filet-*` — aucun littéral de rayon/ombre/police). `index.html` : `style.css?v=8.13`. PC + mobile. Suite header v3 : (2/3) rangée utilitaire `.cartel-utility` + aide « ? » dé-empilées ; puis chantier i18n séparé (bascule globale de langue des noms). Le sous-titre arabe permanent reste pour l'instant (retiré au chantier i18n).

---

- DESIGN / CARTEL — BLOC IDENTITÉ DE FICHE POI (titre serif + liseré, étape 3 du plan de migration, v3.7.278) — implémentation directe du bloc identité conçu dans le handoff Cartel (`ui_kits/heripia-fiche`). `.poi-title-block` → **`.cartel-head`** : (1) eyebrow en petites capitales sépia (`zone · catégorie — sous-type`, séparateurs `·` en encre claire, en `<span>` pour colorer `.sep`/`.subtype` à part) ; (2) **titre en serif** (`--font-serif` = Spectral, fallback Georgia ; `--font-size-2xl`) — le changement le plus visible ; (3) **liseré terracotta 3px à gauche** du bloc (`border-left: var(--liseret-width) solid var(--brand)`), le détail signature du cartel ; (4) la nav in-circuit ‹ 3/12 › migrée `.poi-eyebrow-nav/chev/pos` → `.cartel-nav/-chev/-pos` (petits boutons papier ronds). **Titre arabe (option B, choix Stefan)** : passe d'une **bascule FR/AR** (un seul titre visible) à un **sous-titre arabe PERMANENT** sous le titre FR (façon étiquette de musée) → le bouton « Titre en arabe » du kebab + son handler (`ui-details.js`) sont **retirés**, et `hasAr`/`langId` nettoyés. PC **et** mobile : sur mobile on réutilise les classes cartel dans le header sticky existant (titre + sous-titre arabe ellipsés, header dense) — la structure mobile (titre toujours visible + bouton retour) est conservée, pas déplacée sous le hero. CSS dans `style/cartel.css` ; anciennes règles `.poi-title-block`/`.poi-eyebrow*`/`.poi-title`/`.poi-subtitle-ar`/`.poi-mobile-cap`/`.poi-mobile-title` retirées de panels.css (remplacer = nettoyer). `index.html` : `style.css?v=8.12`. Vérifié en preview PC + mobile ; vitest 953 verts, knip 0. Suite du lot Cartel : sections en filets, timeline en mini-cartels, popup, page SEO.

---

- DESIGN / CARTEL — COVER DE CIRCUIT : FIN DE LA REDONDANCE ZONE/ÉTAPES (Option A, v3.7.277) — décision validée avec Stefan après la v3.7.276 : la zone et le nombre d'étapes étaient affichés DEUX fois — en puces sur la photo ET dans le cartel/eyebrow juste en dessous (`zone · N POIs · X km`, lui plus complet : il a aussi la distance et le D+). Métaphore du cartel : l'étiquette va SOUS l'œuvre, pas écrite sur la photo. Donc le cover redevient purement visuel : retrait des puces zone/étapes sur le cover PC (`src/circuit.js`, `.cp-cover-tags`) ET mobile (`src/mobile-poi.js`, `.cc-hero-tags`), dans l'état avec photo COMME sans photo. Le cover ne garde que ce qui lui est propre : le badge « N photos » (puce papier) avec photo, la plaque « Ajouter une photo » sans photo. Toute la métadonnée vit dans le cartel/eyebrow sous la photo (inchangé). Nettoyage : helpers `buildCoverTags`/`buildTags` supprimés, `renderEmptyCover`/`renderEmpty` réduits à la plaque + libellé, règles CSS `.cp-cover-tags`/`.cc-hero-tags` retirées, import `escapeXml` devenu orphelin retiré de `circuit.js` (remplacer = nettoyer). `.cartel-chip` (style/cartel.css) conservée — toujours utilisée par la fiche POI. `index.html` : `style.css?v=8.11`. Vérifié en preview PC + mobile (cover = photo + « 45 photos », ou plaque + « Ajouter une photo » ; eyebrow `MAZRAIA · 7 POIS · 7.4 KM` toujours présent ; zéro pill sur la photo) ; vitest 953 verts, knip 0. Mise en cartel de l'eyebrow lui-même reportée à la PR « bloc identité serif » (pour ne pas retoucher le cover deux fois).

---

- DESIGN / CARTEL — COVER DE CIRCUIT (PC + MOBILE), MÊME PUCE PAPIER QUE LA FICHE (lot Cartel, PR-B, v3.7.276) — suite directe du retour Stefan sur la v3.7.275 : le bug de lisibilité touchait AUSSI le cover de circuit (surface distincte du hero de fiche, non couverte par la PR-A) — les pills « zone » et « N étapes » blanc-translucide étaient illisibles sur photo claire — PLUS le trou « rien sur un circuit sans photo ». Audit multi-agent (4 agents) : le cover PC et le cover mobile sont les 2 SEULES autres surfaces à poser des pills sur une photo (explorer-card opaque, cartes de liste sans fond-photo, grilles photo admin en gradient sombre lisible = hors scope). (1) PUCES (`style/panels.css` + `style/mobile.css`) : `.cp-pill` (PC) / `.cc-pill` (mobile) translucides → `.cartel-chip` papier (réutilise `style/cartel.css` de la PR-A) ; compteur « N photos » (`.cp-photo-count`/`.cc-photo-count`) passé en puce papier. (2) ÉTAT SANS PHOTO (`src/circuit.js` + `src/mobile-poi.js`) : helpers `buildCoverTags` + `renderEmptyCover`/`renderEmpty` → le cover vide affiche désormais une plaque de papier gravé (classes `.empty-plate`/`.empty-icon`/`.empty-label` FACTORISÉES depuis la fiche POI, donc partagées) + « Ajouter une photo » + les puces zone/étapes (avant : `return` anticipé → glyphe nu, aucune info). (3) NETTOYAGE : anciens `.cp-pill`/`.cc-pill`/`.empty-glyph`/`.cc-hero-glyph` supprimés (remplacer = nettoyer). `index.html` : `style.css?v=8.10` (cache-bust). Vérifié en preview PC + mobile sur le circuit même de la capture (Ben Maazouzine : puces « Mazraia » / « 7 étapes » lisibles sur la photo, badge « 45 photos », zéro résidu d'ancienne classe) ; vitest 953 verts, knip 0. Suite du lot Cartel : consolidation de la redondance (zone/compteur affichés en double — pill sur la photo ET eyebrow texte), bloc identité serif, timeline en mini-cartels, popup, page SEO.

---

- DESIGN / CARTEL — PUCE PAPIER + HERO DE FICHE POI (lot Cartel, PR-A, v3.7.275) — corrige le retour Stefan « les infos en bas à gauche du hero ne sont pas lisibles » (anciennes pills blanc-translucide illisibles sur photo claire) ET le trou « pills absentes quand le lieu n'a pas de photo ». (1) PUCE PAPIER : nouveau `style/cartel.css` (début du fichier Cartel, qui s'enrichira aux PRs suivantes) — `.cartel-chip` = corps PAPIER opaque + filet d'encre, la couleur n'est portée QUE par la pastille (`.dot`) ou l'icône → lisible sur N'IMPORTE quelle photo (claire comme sombre) et dans les 4 moments. Variantes `--ok/--warn/--danger/--brand/--info/--muted`. (2) MARKUP (`src/templates.js`) : les tags du hero (état/accès/incontournable/« À évaluer ») passent de `.poi-tag` translucide → `.cartel-chip--*` ; l'overlay d'épingles est désormais rendu dans LES DEUX branches (avec photo ET sans photo), donc les statuts s'affichent même sans photo. (3) HERO retokenisé (`style/panels.css`) : fond papier (`--paper-2`), scrim d'encre du moment (`color-mix`, fini le `rgba` noir en dur), compteur de photos + bouton retour en puces papier, hero VIDE = plaque de papier gravé (motif topographique au filet + libellé « Ajouter une photo ») au lieu des rayures froides. Ancien CSS `.poi-tag` supprimé (remplacer = nettoyer). Vérifié en preview (fond clair injecté : puces lisibles ; hero vide rendu ; plein soleil + nuit) ; vitest 953 verts, knip 0. Suite du lot Cartel (PRs B+) : cover circuit + consolidation de la redondance, bloc identité serif, timeline en mini-cartels, popup, page SEO.

---

- FIX TOPBAR PWA : RECHERCHE + BASCULE DE MOMENT DÉBORDAIENT VERS LE BAS (retour Stefan, v3.7.274) — en PWA installée (`display-mode: standalone` / `window-controls-overlay`) la topbar est compactée à 40px, mais `#search-input` et la nouvelle bascule de moment (`.hw-moment-toggle`, ajoutée en v3.7.273) restaient à 44px → elles dépassaient de ~2px SOUS la topbar (dans la carte). Compaction ajoutée au bloc `@media (display-mode: standalone)` (qui couvre aussi WCO) : recherche 32px, segments 28px + padding 2px → ~32-34px, dans les 40px. Navigateur (topbar 72px) et plein écran (Fullscreen API → `display-mode: fullscreen`, topbar 72px) inchangés. Vérifié par mesure (topbar 40px forcée : +2px de débordement avant, −3/−4px après = à l'intérieur). NB : le débordement était VERTICAL, pas horizontal (fausse piste largeur écartée).

- DESIGN : REFONTE « PLEIN SOLEIL » — LISIÈRE DISSOUTE + 4 MOMENTS (chantier cohérence design PR1, v3.7.273) — on « sentait les couches » (topbar anguleuse héritée, ~19 rayons, bleu Tailwind étranger, bande sombre recopiée 4×). (1) LISIÈRE : la topbar n'est plus une dalle (dégradé bleu nuit + `color:#fff` + ombre de carte) mais une lisière de papier dissoute (fond `--brand-surface`, filet d'encre 1px, sans-fond façon Maps), identique PC/mobile ; reliquats morts supprimés (`.app-title`/`.separator`/`.zones-menu`). (2) 4 MOMENTS : `style/tokens.css` repeint sur l'axe de la lumière — matin / plein-soleil (DÉFAUT) / ombre (ex-« oasis », nom neutre toute destination) / nuit — couplé à `prefers-color-scheme`, migration des anciens noms (maritime→plein-soleil, desert→matin, night→nuit), TOUS les noms de tokens historiques conservés en ALIAS PARESSEUX → aucun consommateur cassé. (3) UNE GRAMMAIRE : les 4 copies manuelles de la bande sombre (`.focus-bar`, `.osm-pass-actionbar`, `.md-rail-head`, `.ap-bar`) unifiées sur `--brand-surface`/`--on-brand-*` (papier en clair, sombre en nuit) ; parité mobile (`.m-top` papier dissous). (4) PALETTE terracotta (marque) + azur/olive (accents), surfaces sable, encre brun-noir — plein soleil visé AAA (audit AC3/AC4). (5) RETOURS STEFAN : bande du haut unifiée (onglets sidebar = même papier que la topbar), gutter des listes resserré 12→8px, code pays « TN/MA » blanc sur carré terracotta (au lieu de l'émoji-drapeau au rendu Windows incohérent), sélecteur de thème → bascule de moment SEGMENTÉE ÉMOJI persistante (🌅☀️🌿🌙) dans la lisière (ancien dropdown −119 lignes), filet hairline 1px cohérent partout. Vérifié en preview réelle (plein soleil + nuit). RESTE (PRs suivantes) : le Cartel (fiche façon étiquette de musée + redondance fiche), serif Spectral, tuiles carte chaudes, snap rayons/ombres + verrou stylelint en CI.

- CARTE : FOND « PLAN (FR) » — NOMS EN FRANÇAIS (retour Stefan, v3.7.272) — les 3 fonds existants (Voyager/Plan/Satellite) sont des tuiles RASTER pré-rendues serveur : en Tunisie elles affichent le `name` local OSM, donc en arabe (نابل, الحمامات…), sans réglage de langue possible. Ajout d'un 4ᵉ fond sélectionnable « Plan (FR) » servi par OSM France (`{s}.tile.openstreetmap.fr/osmfr`), dont le style privilégie l'étiquette `name:fr` quand elle existe → noms en écriture latine/française là où OSM les renseigne (vérifié en preview sur Djerba : Midoun, Houmt Souk, Guellala… ; sur Nabeul/Hammamet : Nabeul, Dar Chaâbane, Korba, « Golfe de Hammamet » traduit). Voyager reste le fond PAR DÉFAUT (base claire calée sur les marqueurs ocre, décision 20/05) ; « Plan (FR) » est une option du sélecteur de couches, à choisir quand on veut lire les noms. Plomberie : domaine `*.tile.openstreetmap.fr` ajouté à la CSP (img-src + connect-src) — SANS quoi le fond marcherait en dev Vite mais serait bloqué en prod (leçon styles inline/CSP) — et au cache SW (vite.config.js, runtime caching des tuiles) ; maxNativeZoom=19 (même garde anti-grisé z20 que Plan/Voyager) ; attribution OSM France ajoutée à la modale Mentions légales (données toujours © OpenStreetMap contributors, ODbL). Reste arabe par DÉFAUT (Voyager) : bascule manuelle via le contrôle de couches.
- SCOUT : RÉOUVERTURE AUTO APRÈS CRÉATION D'UNE DESTINATION (retour Stefan, v3.7.271) — suite du flux inversé v2 (#834) : devoir relancer le Scout à la main sur le brouillon qu'on vient de créer était une friction. Le reload de bascule reste inévitable (changer de destination = reboot complet de Heripia), mais le Scout se rouvre désormais DESSUS automatiquement. `createDestinationDraft` (scout.js) bascule en `?map={id}&scout=1` ; main.js lit `?scout=1` (même patron que `?poi=`/`?import=` : URL nettoyée via replaceState + délai 500 ms pour laisser la carte finir) et rappelle `startScout()`. Desktop + admin only (le Scout n'existe pas sur mobile ; `startScout` regarde de toute façon `state.isAdmin`/`currentMapId`) ; échec d'import dynamique → warn, pas de crash. Toast de création reformulé (« le Scout rouvre dessus » au lieu de « relance le Scout »). Vérifié en preview : création Hammamet → bascule → Scout rouvert seul sur le brouillon vide (statut « brouillon local »), URL nettoyée, prêt à tracer/scanner.
- SCOUT V2 : FLUX INVERSÉ — CRÉER LA DESTINATION AVANT DE SCOUTER (validé Stefan, v3.7.270) — l'ancien toggle Nouvelle/Repasse faisait bifurquer la CAPTURE selon un état invisible : une capture « repasse » avec la carte sur Hammamet injectait ~50 lieux dans Djerba (constat terrain 12/06 ; enquête preview : le flux « Nouvelle » lui-même était sain de bout en bout, le mélange venait de résidus locaux pré-#832 — mais la classe d'erreur restait ouverte). Désormais : (1) « NOUVELLE DESTINATION… » = bouton du panneau (formulaire replié : recherche Nominatim → « Créer le brouillon ») qui crée un brouillon local VIDE (`createDestinationDraft`, zéro POI) puis bascule dessus (?map={id}) — bounds = fenêtre élargie à MIN_VIEW_KM (25 km) autour du lieu (la bbox Nominatim d'une ville = son bâti, trop serrée pour le cadrage d'une destination) ; zones OSM récupérées à la création comme avant. (2) LE SCOUT MOISSONNE TOUJOURS LA DESTINATION ACTIVE — chemin de capture unique via addPoiFeature → `customPois_{id}`, le canal canonique lu par la publication (cf. en-tête publish-destination.js : le snapshot draftGeoJSON_{id} n'est écrit qu'à la création) ; autant de passes de scan/capture qu'on veut. (3) GARDE-FOU GÉOGRAPHIQUE : une boîte qui ne TOUCHE PAS les bounds de la destination active bloque le scan avec le geste correct en toast (chevauchement partiel accepté — étendre une dest est légitime ; bounds illisibles → pas de garde). NETTOYAGE (remplacer = nettoyer) : `_mode`/`setMode`/toggle markup + CSS `.scout-mode*` et `.is-repasse` (boîte ambre) supprimés ; `zoneNameForPoint` (osm-zones.js) retiré — il ne servait qu'à l'ancien chemin (la Zone d'un candidat vient de getZoneFromCoords sur les zones de la dest active, chargées au boot depuis draftZones_{id}). Statut de la dest affiché dans le panneau (publiée / brouillon local / brouillon GitHub).
- SCOUT : NAVIGATION LIBRE + FENÊTRE MINI AU GÉOCODAGE (retours test Hammamet, v3.7.269) — deux frictions relevées par Stefan au premier test terrain du Scout in-app. (1) TRACÉ ARMÉ PAR BOUTON : la couche de tracé plein écran s'armait d'office dès qu'aucune boîte n'existait → elle masquait les contrôles Leaflet (zoom/couches/recentrer, sous l'overlay z-1500) et bloquait pan/molette : impossible de cadrer la carte AVANT de dessiner. Désormais l'état par défaut = navigation libre ; nouveau bouton « Tracer la zone » dans le panneau (section « Zone à moissonner ») qui arme le crosshair (pattern standard des outils SIG) — armé : « Annuler le tracé » (toggle) ; boîte posée : « Retracer la zone » (efface + ré-arme) ; un glisser trop court laisse armé. « Réinit. » et une nouvelle recherche désarment (retour navigation). (2) FENÊTRE MINI 25 KM : en mode Nouvelle, `fitBounds(bbox Nominatim, maxZoom 16)` cadrait au ras de la ville (la bbox d'une ville = son bâti) → nouveau `minViewBounds` garantit une vue d'au moins 25 km de côté (MIN_VIEW_KM) centrée sur le lieu, y compris pour un résultat ponctuel sans bbox (ex-setView zoom 14). `_geoBBox` (bounds du futur brouillon) reste la bbox Nominatim d'origine, seule la VUE est élargie.
- RESET DU BROUILLON HAMMAMET (avant test du nouveau Scout, v3.7.268) — l'ancien brouillon Hammamet (scan Scout brut : 53 lieux, 0 description, 0 circuit, beaucoup « À définir ») publié sur GitHub en status `draft` est retiré pour repartir à blanc. Supprimés : `public/hammamet.geojson`, `public/hammamet-zones.geojson`, `public/circuits/hammamet.json` + entrée `hammamet` de `destinations.json` (libère l'id pour que le Scout reparte dessus, sinon `hammamet-2`). Archivé hors dépôt dans `.claude/archive/hammamet-scout-v1/` (et dans l'historique git). Zéro impact user (Hammamet n'était visible qu'en admin via badge « Brouillon »). À NE PAS oublier (volet 2, hors PR) : vérifier/nettoyer un éventuel résidu de brouillon LOCAL `draftDestinations['hammamet']` dans l'IndexedDB admin (inspection avant suppression) — sinon l'id reste pris côté navigateur. Le générateur SEO ignore Hammamet (draft), 56 pages Djerba inchangées.
- SEO PAGES LIEU : LIEN SOURCE RACCOURCI (suivi F3, v3.7.267) — l'URL Source s'affichait en entier sous la fiche (longue, illisible). Désormais le LIBELLÉ est le domaine sans `www.` (ex. `djerba.holiday`), le `href` garde l'URL complète — exactement comme `renderSource` côté app (templates.js). `safeSourceUrl` → `safeSource` qui renvoie `{url, domain}` (garde http(s) only, leçon S4, inchangée). CSS `.source` : `word-break:break-all` retiré (plus d'URL longue à casser). Test mis à jour (libellé=domaine, href=URL complète).
- PAGES SEO PAR LIEU + SITEMAP + DEEP-LINK (audit F3/SEO1, v3.7.266) — le plus gros levier SEO de l'audit : le contenu vivait en JS sur une URL unique, les ~300 lieux n'existaient pour aucun moteur. (1) GÉNÉRATEUR : `scripts/generate-poi-pages.mjs` (hook npm `postbuild`, tourne en CI après `vite build` → déploiement auto sans toucher au workflow) produit `dist/lieux/<mapId>/<slug>/index.html` pour chaque lieu des destinations `published` avec description ≥ 80 car. (décision Stefan « pas de thin content » : sur 160 descriptions non vides, 104 sont < 80 car. et quasi toutes des notes de travail « Infos à rechercher » — le seuil filtre juste ; 56 pages aujourd'hui, le nombre grandit seul avec l'enrichissement). Chaque page : title/meta description (155 car. coupée au mot), canonical, OG complets, JSON-LD TouristAttraction (geo, alternateName arabe, containedInPlace zone), nom AR dir=rtl, photo si publiée (vérifiée sur disque), État/Accès/Horaires/Téléphone, Source (garde http(s) only, leçon S4), CTA « Voir sur la carte Heripia ». Données échappées partout (texte brut + \n→<p>, même traitement que templates.js). + `dist/sitemap.xml` (lastmod = mtime geojson) + `dist/404.html`. (2) PRELAUNCH=true : pages noindex tant que l'app l'est — basculer à false LE MÊME JOUR que le retrait du noindex d'index.html, puis soumettre le sitemap (checklist SEO2, documenté en tête de script). (3) DEEP-LINK `?poi=HW_ID` (main.js, patron ?import= : URL nettoyée + délai post-carte) : ouvre la fiche du lieu (PC sidebar / mobile plein écran, openDetailsPanel commun) + nouvel événement `map:focus-poi` dans map.js (flyTo+highlight, geste du résultat de recherche — événement car main ne doit pas importer la carte) ; introuvable = toast warning, zéro crash. (4) SW : `/lieux/` ajouté au navigateFallbackDenylist — sinon un user avec l'app installée cliquant un lien Google recevrait index.html au lieu de la page. Post-build = rien au precache (29 entrées inchangées). 14 tests vitest (slugify/dédup/seuil/échappement/JSON-LD/sitemap) ; vérifié Playwright : page statique rendue, deep-link PC+mobile, toast introuvable.
- PRÉPARABLES SEO PRÉ-LANCEMENT (audit SEO2 items 2+5, v3.7.265) — ce qui se prépare AVANT le jour J (le retrait du noindex de l'app, le sitemap et le re-scrape FB restent pour le lancement). (1) NOINDEX OUTIL : `public/zones-viewer.html` (seule page outil encore déployée depuis la réunification #819) reçoit `<meta name="robots" content="noindex, nofollow">` — sinon l'outil admin devenait indexable le jour où le site le devient. (2) MANIFEST ENRICHI : `lang: "fr"` + `dir: "ltr"` + 3 `screenshots` pour la fiche d'installation riche (desktop carte+sidebar 1280×800 form_factor wide ; mobile liste Mes Circuits + suivi de circuit carte/tracé/point bleu 780×1688 narrow). (3) CAPTURE REPRODUCTIBLE : nouveau `scripts/capture-screenshots.mjs` (npm run screenshots, Playwright déjà dans le projet) — neutralise modale bienvenue + bandeau install via localStorage, géoloc factice posée sur le départ du circuit Sidi Zayed pour que le point bleu tombe SUR le tracé. Découverte : le boot mobile = liste Mes Circuits, PAS la carte (d'où l'échec initial de l'attente des tuiles) ; la carte mobile se capture via fiche → « Suivre ce circuit ». (4) PRECACHE PROTÉGÉ : `public/screenshots/` ajouté aux globIgnores du SW (~820 Ko fiche d'installation only, inutile hors-ligne — même logique que og-image, leçon P1). Doc : ligne `npm run screenshots` ajoutée à la table du README.
- DOCUMENTATION DU DÉPÔT (audit M1+M2, v3.7.264) — (M1) ARCHITECTURE.md créé à la racine (versionné) : paradigme « chaque utilisateur est admin de sa copie » + zéro backend (GitHub Pages, API Contents PAT admin, Gist user), les 4 dimensions de données (patrimoine / admin éphémère / préférences user / techniques) avec le cas dual userData↔PERSONAL_KEYS et le flux brouillon→publication→officialisation, carte des ~100 modules src/ par domaine (boot, carte, circuits, routing, photos, CC Admin, Mode Données/Scout, sync, mobile, UI), conventions (setters state, openHwModal sans stacking, classes CSS only, appIcons, bump version+CHANGELOG, CI vitest+knip) et les 7 pièges connus distillés de l'historique (CSP prod≠dev, deux éditeurs de lieu, icône hors pack invisible, taxonomie 2 sources, retard GitHub Pages vs API, style.css sans HMR, viewer photo gelé). (M2) README réécrit : titre « Heripia » (note sur le nom historique du dépôt), URL de clonage corrigée (Stefanmartin1967/, pointait vers StefanoWorld/ — mauvais propriétaire depuis des mois), « 60 modules » → ~100, dossier circuits/ racine fantôme → public/circuits/, app-events.js disparu retiré de l'arborescence, ajout des features absentes (routing BRouter, suivi GPS, HEIC, rognage, Mode Données/Scout in-app), script knip ajouté à la table, section Documentation pointe vers ARCHITECTURE.md + docs/ au lieu de .claude/ (non distribué = impasse pour un cloneur). Constat au passage : M3/A1 (changelog) était déjà réglé depuis v3.7.250. Doc only, zéro code touché (hors bump version).
- TIMELINE PC : ICÔNES DU PACK POI (fix moon-star, v3.7.263) — la pastille catégorie des étapes de circuit PC (.step-cat, circuit-view.js) utilisait un mapping Lucide LOCAL resté sur la taxonomie v1 (« Borj », « Caravansérail », « Fontaine »… disparus) avec 'moon-star' pour Mosquée/Synagogue — icône JAMAIS enregistrée dans appIcons → invisible à l'écran + warning console en boucle à chaque rendu. Au lieu d'ajouter MoonStar (pansement), branchement sur le PACK D'ICÔNES HERIPIA (Claude Design, 27 icônes ocre) comme partout ailleurs : helper `getStepCategoryDisplay(feature)` extrait de mobile-poi.js vers poi-icons.js (source unique), consommé par les DEUX timelines (PC + mobile). PC gagne : icônes ocre sous-type-aware (mosquée à minaret ≠ à coupoles), Synagogue avec SA vraie icône, variantes de pastille alignées mobile (amber resto/café/curiosité/pâtisserie/salon de thé, brand lieux de culte), « Resto » raccourci. Fix au passage : le label lit userData AVANT properties, comme le lookup d'icône (sinon POI recatégorisé = nouvelle icône + ancien label). Mobile : zéro changement de comportement (pur déplacement de code). Leçon PowerShell : JAMAIS Get-Content|Set-Content sur un fichier source UTF-8 (double-encodage mojibake, rattrapé par git checkout avant commit) — renommages via Edit only.
- ACCESSIBILITÉ MODALES (audit Vague 4 PR3 : AC1, v3.7.262) — piège de focus CENTRAL dans openHwModal (modal.js), patron copié de help-popover.js, donc TOUTES les modales V2 (HW + helpers showConfirm/showPrompt/showAlert/showCustomModal) en bénéficient d'un coup. (1) SÉMANTIQUE : .hw-modal porte role="dialog" + aria-modal="true" + aria-labelledby vers l'id unique du titre (hw-modal-title-N) — avant, une modale N'EXISTAIT PAS pour un lecteur d'écran. (2) PIÈGE TAB : handler keydown global trapHwFocus (même sélecteur FOCUSABLE que help-popover, filtre offsetParent) — Tab/Shift+Tab bouclent dans la modale, focus échappé ramené au 1er focusable. (3) FOCUS INITIAL : 1er focusable hors croix (input d'un prompt, Annuler d'un confirm), repli croix. (4) RESTITUTION : l'élément actif à l'ouverture est refocusé à la fermeture (s'il est toujours connecté) ; chaînage A→B (stacking interdit) = B hérite de l'ouvreur de A si le focus vivait dans A. (5) SUSPEND/RESUME : trap handler + ouvreur suivent la modale suspendue comme l'escape handler (suspendHwModal/resumeHwModal, pattern photo-batch/richEditor). Viewer photo (.is-photo-viewer via openHwModal) vérifié intact en preview : navigation + fermeture OK, le trap n'intercepte QUE Tab. 6 tests jsdom ajoutés (rôles, ids uniques, focus entrant, restitution, ouvreur disparu, chaînage) — le CYCLAGE Tab lui-même non testable sous jsdom (offsetParent null), vérifié en preview. CLÔT LA VAGUE 4 de l'audit (AC1+AC2+AC3+AC5+AC6+AC8 ; restent AC4 thème plein soleil = chantier design Vague 5, AC7 zoom = statu quo documenté).
- ACCESSIBILITÉ LISIBILITÉ & CIBLES TACTILES (audit Vague 4 PR2 : AC3+AC5-visuel+AC6, v3.7.261) — (AC3) CONTRASTE TEXTE SECONDAIRE : --ink-soft foncé sur les 3 thèmes sous le seuil AA 4.5:1, ratios CALCULÉS (WCAG) sur surface ET surface-muted : maritime #627D98→#587088 (5.14/4.65), desert #8A6F5E→#816858 (4.92/4.53), oasis #528C7C→#457568 (5.02/4.72) ; night déjà conforme (9.89). Teintes conservées (HSL, luminosité seule abaissée). (AC5-visuel) TOASTS LISIBLES : blanc sur --ok/--warn/--brand clairs = 2.1-2.5:1 (et 1.92 en oasis pour info sur --brand vert) → fonds foncés via tokens *-bg : ok-bg/danger-bg existants + NOUVEAUX --warn-bg #B45309 (5.02) et --info-bg #1D4ED8 (6.7), identiques sur les 4 thèmes comme ok-bg/danger-bg. DURÉE HONORÉE : le fadeOut CSS au délai figé 3.5s neutralisait tout duration>4s → délai piloté par --toast-fade-delay (toast.js, CSSOM setProperty compatible CSP) + remove() sur animationend fadeOut (un seul mécanisme, l'ancien setTimeout doublon supprimé). (AC6) CIBLES TACTILES : dock mobile boutons 40→48px (padding 8→12px, compensé par .mobile-nav 10→6px = hauteur de bandeau inchangée, zéro carte perdue) ; pager fiche circuit mobile (.cc-pager-btn, seul usage réel = variante in-eyebrow 20×20 !) → hit-slop par pseudo-élément ::after inset -8px/-12px = zone 44×44 SANS changer le visuel compact de la ligne eyebrow ; .cp-act barre circuit PC 36→44px réels (tablette, la sidebar a la place). Cache-bust style.css 7.97→7.98. PR3 = piège de focus openHwModal.
- ACCESSIBILITÉ LECTEUR D'ÉCRAN (audit Vague 4 PR1 : AC2+AC5-aria+AC8, v3.7.260) — (AC2) DOCK MOBILE : les 5 boutons de navigation n'avaient AUCUN nom accessible (pas d'aria-label, libellés <span> en display:none donc retirés aussi de la lecture d'écran) → aria-label sur chaque bouton (Circuits / Rechercher / Filtres / Ajouter un lieu / Menu) + aria-label="Navigation principale" sur le <nav>. (AC5-aria) TOASTS : conteneur #toast-container en aria-live="polite" + role par toast dans toast.js (erreurs = role="alert" interruptif, le reste = role="status") — une erreur de sauvegarde est désormais annoncée. Le volet VISUEL d'AC5 (contraste fonds, durée hardcodée 3.5s) = PR2. (AC8) IMAGES & CLAVIER : alt informatif sur les vignettes de la grille photo (« Photo N — nom du lieu », ui-photo-grid) et sur l'image du viewer (« Photo N sur M » — attribut sémantique pur, viewer par ailleurs gelé cf. #642-645) ; champ recherche topbar aria-label (placeholder seul = pas un label) ; cartes de circuits OUVRABLES AU CLAVIER : PC va-card (role="button" + tabindex=0 + aria-label + Enter/Espace, idiome cc-card/hero POI) et mobile mc-card (avait déjà role/tabindex SANS handler clavier — patron à moitié câblé, Enter/Espace branché). Garde e.target !== card sur les 2 keydown : un Enter sur le bouton « fait » interne conserve son activation native (sinon preventDefault la cassait). Zéro changement visuel. PR2 = contrastes+cibles tactiles, PR3 = piège de focus openHwModal.
- RÉSIDUS DE DÉPÔT (audit A6, v3.7.259) — (1) `Scout_Hammamet.geojson` supprimé de la racine : vérification faite avant suppression (condition de l'audit) — le fichier ne contenait PAS la donnée du brouillon Hammamet mais un scan OSM d'ALGER mal nommé (258 features, centre lat 36.776/lon 3.025, 0 nom commun avec les 53 lieux du brouillon publié), commité tel quel dans le dépôt initial de février. Donnée brute OSM reproductible à la demande via le Scout in-app si Alger devient une destination. (2) `test-results/.last-run.json` retiré du suivi Git (`git rm --cached`) — il était tracké AVANT la règle `.gitignore` (test-results/), qui le couvre désormais.
- PERF MOBILE & DISQUE (audit P5+P6, v3.7.258) — (P5) ENREGISTREMENT PHOTOS : saveCluster compressait TOUTES les photos d'un groupe en même temps (Promise.all) — sur 20-30 photos de 12 Mpx, pic mémoire transitoire de plusieurs centaines de Mo (un canvas plein format par photo), risque de fermeture d'onglet sur mobile modeste. Désormais mini-pool de 3 compressions simultanées (ordre préservé par écriture indexée) ; les autres chemins de compression (desktopMode, ui-photo-grid) étaient déjà séquentiels. (P6) COPIES DE SECOURS : lastGeoJSON (~1,2 Mo) + lastZones_{dest} étaient réécrits en IndexedDB à CHAQUE démarrage, même inchangés. Désormais : mémorisation de « destination|ETag » GitHub Pages (clés lastGeoJSON_etag / lastZones_etag_{dest}, même store que la copie → vider la base invalide tout ensemble) — etag identique = écriture sautée ; pas d'etag ou différent = écriture comme avant (sûr). Et un boot servi par le repli hors-ligne ne réécrit plus la copie qu'il vient de lire (geojsonFromCache).
- LOT ROBUSTESSE (audit R5, v3.7.257) — 4 angles morts fermés. (1) CAPTURE SCOUT : la boucle addPoiFeature est sous try/catch — un échec d'écriture IndexedDB en cours de capture laissait des pastilles disparues et des POIs manquants SANS MESSAGE ; désormais toast d'échec partiel « X/N enregistrés » + nettoyage standard (un re-scan re-marque les déjà-écrits en doublons, pas de double capture). (2) IMPORTS DYNAMIQUES : 9 `import().then()` fire-and-forget sans `.catch` (circuit.js ×2, gpx.js, sync.js, fileManager.js ×2, ui.js ×2, ui-photo-grid.js) — un chunk manquant juste après un déploiement rendait le clic muet ; désormais console.warn partout + toast « Chargement impossible — actualise l'application » sur les actions utilisateur directes (modale Sauvegarder, viewer photo) ; le bouton soutien ouvre la page même si recordSupportClick échoue. (3) BOOT : une réponse serveur en erreur (500 CDN) sur le geojson déclenche désormais le MÊME repli hors-ligne qu'un échec réseau (avant : « Impossible de charger la carte » direct). (4) PUBLICATION : le callback onProgress de publishDraftToGitHub / officializeDestination (jamais branché) alimente des toasts d'étape — l'admin voit « Publication des lieux… », « Publication des zones… » au lieu d'un toast statique de 8 s.
- SUIVI GPS : FIN DU POINT BLEU FIGÉ (audit R4, v3.7.256) — pendant le suivi de circuit mobile, une perte GPS LONGUE (ruelle couverte, localisation coupée en route) laissait le point bleu figé sur la dernière position connue, sans aucun signal — le marcheur pouvait se croire « là ». Désormais : compteur d'échecs consécutifs après le 1er fix (`_gpsErrStreak`, mobile-follow.js) → au 3e échec (~45 s, timeout watchPosition 15 s), retour à l'état « Recherche du signal GPS… » et retrait du point obsolète ; le prochain fix rebascule en live. Permission révoquée EN COURS de suivi (err.code 1) → invite « Autoriser » immédiate (aucun fix ne reviendra). Les pertes momentanées (< 3 échecs) restent silencieuses, comme avant.
- SUPPRESSION DES 2 APPS AUTONOMES (réunif, CLÔTURE, v3.7.255) — fin du chantier réunification : suppression de `tools/scout.html` (remplacé par le Scout in-app `src/scout.js`) et de `history_walk_datamanager/` (remplacé par le Mode Données in-app `src/mode-donnees.js`). Retiré : les 2 inputs Rollup + le denylist SW `/tools/` (vite.config.js), 3 entrées knip (scout.html + 2 DM) + le glob projet DM, 3 tests qui importaient du DM (`datamanager_utils`, `datamanager_dirty_flag`, `isPointInPolygon` — coverage `generateHWID`/`isPointInPolygon` déjà dans `tests/utils.test.js`). VOLET INTÉGRATION DM mort retiré du CC admin : carte « Data Manager » + handler, bandeau « brouillon DM non publié » (+ `openDm`/`btn-goto-dm`), stat « Brouillons depuis le DM » (`dm_has_unpublished_changes` que plus personne n'écrit), garde-fou cross-app pré-publication (admin-control-center). CSS : règle orpheline `.cc-banner-link` retirée, grille `.cc-stats` 4→3 colonnes (Lieux/Photos/Circuits). Docs : README + commentaire leaflet-controls.css. Commentaires de provenance « porté de tools/scout.html » conservés (historique). knip vert + grep CSS (parade code-mort).
- OFFICIALISER UNE DESTINATION (réunif, v3.7.254) — bouton CC › Outils « Officialiser cette destination » (icône Globe), visible UNIQUEMENT sur un brouillon GITHUB (`status:"draft"` et PAS `custom` local → Hammamet, ou une dest scoutée déjà poussée). `officializeDestination(id)` (publish-destination.js) : lit destinations.json FRAIS via Contents API → flip `draft→published` → re-push. GARDE-FOU CANDIDAT (option C) : les candidats Scout non curés restants sont (1) PRÉSERVÉS en local (`customPois_{id}` + setCustomFeatures → réapparaissent au boot comme customFeatures candidate:true sur la dest publiée = état C1a « Repasse », curables ensuite ; #816 les garde hors public jusqu'à curation) et (2) ÉCARTÉS du geojson public (generateMasterGeoJSONData keepCandidates:false). On re-pousse TOUJOURS le geojson (une curation locale pas encore « Tout publiée » laisserait sinon des candidate:true au flip) → officialise depuis l'appareil à jour. Ordre geojson D'ABORD, destinations.json EN DERNIER. Reflet local immédiat (state.destinations status:published → badge Brouillon retiré) ; autres appareils après déploiement Pages (~1-2 min). Pré-vol : token + dest active + pas déjà publiée + présente sur GitHub. 8 tests. La SUPPRESSION des 2 apps (DM + scout.html) reste le dernier reste du chantier réunification.
- CURATION CANDIDAT MULTI-APPAREILS (réunif PR1, v3.7.253) — « Valider » un candidat marchait via `customPois` (capture LOCALE), mais cassait sur un BROUILLON GitHub où le candidat est un feature de BASE (re-fetché du geojson à chaque boot, hors customFeatures). Cause : flag lu de 2 façons incohérentes — mergé (filtre) vs brut (badge marqueur + 2 gardes du diff). Fix : helper `isCandidate(feature)` (utils, via getPoiProp overlay = userData prime sur patrimoine), SOURCE UNIQUE des 5 lecteurs (filtre data.js, badge map.js, ligne RichEditor, gardes admin-diff-engine ×2). `curateCandidate` branche selon l'origine : customFeature → delete flag + customPois (inchangé) ; feature de base → `userData.candidate=false` + savePoiData + `admin:poi-edited{update}` → le diff le piste (affiché « Curation : Candidat → Validé »), « Tout publier » repousse le geojson et admin-geojson retire la clé `candidate:false` → geojson curé propre. Cycle multi-appareils complet : scan → Capturer → Publier brouillon GitHub → curer depuis N'IMPORTE QUEL appareil → Tout publier. Tests : isCandidate (utils), strip (admin-geojson), curation diff (admin-diff-engine).
- FIX FUITE CANDIDAT (réunif, v3.7.252) — 4e couche du garde-fou candidat Scout : `generateMasterGeoJSONData(excludedIds, { keepCandidates })` exclut désormais par défaut les POI `candidate:true` (non curés). Les 3 couches existantes (draft:false, reconcileLocalChanges, prepareDiffData) ne protégeaient que le DIFF ; ce générateur régénère le geojson ENTIER → un candidat capturé en « Repasse » sur une destination PUBLIÉE fuyait dans sa source publique au prochain « Tout publier » (et un user normal le voyait, le filtre « à curer » étant admin-only). Désormais : « Tout publier » exclut les candidats si la destination active est publiée, les garde si c'est un brouillon (status:draft, admin-only) ; le push C2b (publish-destination.js) et le compteur du bouton « Publier en brouillon GitHub » passent keepCandidates:true (le brouillon les transporte exprès) ; l'Export Master et tout futur appelant excluent par défaut (sûr). 3 tests admin-geojson ajoutés.
- PROFIL PHOTO PUBLICATION UNIQUE (décision 11/06) — PUBLISH_COMPRESSION 1080px·q0.75 partout (import GPS via compressImage, ex-compressFileToBlob 1600·q0.88 fusionné+timeout transféré ; grille OPTIMIZED alignée ; USER inchangé 1200·q0.8). FIX PUBLICATION BROUILLON (audit R1) — publishDraftToGitHub publie l'état AFFICHÉ via generateMasterGeoJSONData (captures + curation overlay + suppressions), au lieu du snapshot draftGeoJSON_ figé à la création. ROUTING IN-APP PR3 (calque de référence) — import d'une trace GPX (Wikiloc…) via le bouton « Calque » de la barre d'actions du panneau circuit (data-show=create, TOUJOURS visible même à 0 POI car le calque guide la construction) OU le toggle « Calque » du focus → affichée en fond pointillé gris (.route-reference-layer) comme GUIDE VISUEL, en SESSION, n'entre PAS dans le tracé (state.referenceLayer). Chip « Calque de référence · fichier » dans le bloc tracé (retrait) + toggle « Calque » dans la barre du focus (afficher/masquer). Module src/circuit-reference-layer.js (parse trkpt/rtept, drawReferenceLayer/clearReferenceLayer/fitReferenceLayer dans map.js). Icône Layers ajoutée. PR4 = POI à la volée (école A puis B).
- ROUTING IN-APP PR2 (niveau 2 / focus mode) — « Ajuster le tracé » ouvre un plein écran dédié (calqué sur la passe drapeaux : body.circuit-focus-active masque la sidebar, carte plein écran, rail latéral 312px + barre basse + « Quitter »). Points de passage = marqueurs Leaflet draggables (patron access-point-editor) rattachés à un segment POI→POI, en SESSION (state.draftWaypoints, vidés à la sortie ; décision « trace finale seule »). Routing SEGMENT PAR SEGMENT (circuit-routing.routeCircuit étendu : 1 appel BRouter par segment POI→POI, vias = ancres+waypoints) → state.routeSegments [{coords,ok,distanceKm}] ; re-route du seul segment touché au dragend/ajout/suppression ; realTrack = concat, sauvegardé UNIQUEMENT à « Terminer » (Quitter = annule via snapshot d'entrée). Échec partiel (D) : un segment non routable retombe en ligne droite (.route-fail-segment pointillé) + note furet « un passage est resté en ligne droite » dans le trace-status. Trace-status restructuré : « Ajuster le tracé » (primaire → focus) + menu « Plus » (Re-tracer / Exporter GPX / Affiner dans GPX Studio) ; l'item « Importer une trace de référence » = PR3. Focus entry sans routeSegments (circuit chargé) : split du realTrack par ancres (préserve la géométrie sauvegardée). Garde circuitFocusActive sur le clic POI (map.js) pour ne pas modifier la séquence en focus. Styles portés du handoff Claude Design (style/circuit-focus.css, vrais tokens Heripia). Icône Waypoints ajoutée à appIcons. CYCLE DE VIE DU TRACÉ (05/06, validé Stefan) : bouton d'itinéraire UNIQUE contextuel (« Tracer l'itinéraire » vierge → « Éditer l'itinéraire » tracé). convertToDraft NE jette plus le realTrack (éditer conserve le tracé) et pose state.routeBasisKey (clé poiIds ordonnée). Si la séquence de POIs change ensuite → tracé PÉRIMÉ : badge ambre « Tracé à mettre à jour » + Re-tracer (isStale = routeBasisKey ≠ poiIds courants). map.js : le tracé réel s'affiche dès qu'il existe, vol d'oiseau SEULEMENT si périmé (isRouteStale remplace l'ancien `!editingMode` qui forçait le vol d'oiseau en édition — réflexe GPX Studio). Ajout de point de passage = clic GAUCHE sur la carte en mode « Ajouter un point » (plus de hit-line transparente opacity:0 qui ne captait pas les events SVG ; on rattache au segment le plus proche). Clic droit (création POI desktopMode) neutralisé pendant le focus. activeCircuit() routing = les 2 listes (officiel édité inclus). Tests convertToDraft (garde le tracé + pose la base). Vocabulaire : « Modifier le circuit » (crayon = POIs/nom) ≠ « Éditer l'itinéraire » (le tracé). PR3 = découverte (créer un POI à la volée pendant l'édition + ajout AUTO à la séquence + re-trace ; calque de référence Wikiloc ; clic droit réactivé proprement en focus).
- CHANTIER Scout — remplacement total bbox (Sud/Ouest/Nord/Est) par Centre + Rayon (km, entier sans décimales). Plus intuitif que 4 coordonnées (retour Stefan 01/06/2026). Mode destination connue : centre = barycentre des zones, rayon = demi-diagonale de l'enveloppe (computeCenterAndRadius). Mode manuel : geocoding Nominatim renvoie lat/lon + rayon dérivé de la boundingbox. Overpass `(around:radiusM,lat,lon)` à la place de `(s,w,n,e)`. fetchZonesViaOverpass reçoit une bbox circonscrite au cercle (bboxFromCircle) — Overpass ne sait pas faire around: sur des relations admin. Sauvegarde destinations.json : bounds reconstruites depuis le cercle (compat préservée), startView.center = (lat,lon) directs. Scout est un outil HTML standalone (tools/scout.html), pas de vitest.
- CHANTIER enrichissement photo — point 4 : catégorisation au batch dans la modale d'import (ui-photo-batch.js). 2 nouveaux boutons sur la barre cluster pour les POIs RATTACHÉS : (1) « Catégoriser » → popover compacte 4 selects (Catégorie / Sous-type / État / Accès), réutilise taxonomy.js (getSubtypes/getStates/getAccessValues). Préremplie avec les valeurs actuelles (correction permise). Diff écrit via saveTaxonomyBatch atomique : userData merge → savePoiData unique → eventBus admin:poi-edited → schedulePush → applyFilters + refreshMapMarkers. Ne persiste QUE les champs vraiment modifiés (pas de pollution diff CC). Badge orange .pb-act.is-warn sur le bouton si POI mal catégorisé (Catégorie === 'A définir' OU sous-types disponibles + Sous-type vide). Tooltip natif montre la cat courante. (2) « Éditer la fiche » → ouvre RichEditor.openForEdit(poiId) via suspendHwModal/resumeHwModal (pattern handleCreatePoi) ; à la fermeture refresh modale (badge / tooltip si cat changée via RichEditor). Workflow : photos sous les yeux = bon moment pour catégoriser ET pour décrire les POIs marquants. Rythmes assumés séparés : popover rapide (batch) vs RichEditor (édition riche). Icône Tags ajoutée à lucide-icons.js (appIcons). CSS popover dans photo-batch.css (.pb-cat-popover position:fixed z:10080 280px, tokens HW). Cache-bust 7.67→7.68.
- CHANTIER drapeaux d'accès v2 PR 4/5 — Multi-drapeaux à la création/édition de circuit (admin). Nouveau src/circuit-flags.js standalone (~270 lignes) : écoute window event circuit:updated pour synchroniser les markers Leaflet (drapeaux .hw-flag) avec state.currentCircuit. Affichage des drapeaux des POI off-route ayant un accessPoint connu (status osm/moved), drag bascule en moved + marque dirty, save groupé via commitDirtyFlags() appelée par saveAndExportCircuit avant applyFilters. Règle CADENAS en édition d'un circuit existant : markEditingStart(state.currentCircuit) appelée dans convertToDraft juste AVANT setEditingMode(true) → fige snapshot _initialPoiIds → drapeaux des POI préexistants en --locked (rouge + badge cadenas) non-draggables, tooltip native title="Verrouillé — modifie depuis la fiche POI". POI ajoutés après l'entrée en édition = libres. Import statique side-effect dans circuit.js pour POSER le listener circuit:updated dès le boot (sinon premier circuit:updated raté). Tests : 7 jsdom (mocks Leaflet) couvrant markEditingStart, getDirtyCount, teardown idempotent, commitDirtyFlags vide. 899 tests verts total. Cache-bust 7.66→7.67.
- Fix barre passe largeur stable + tooltip nom POI complet (retour Stefan post #711) : .osm-pass-actionbar min-width 720px (ne rétrécit plus), .meta max-width 240→320 + margin-right auto (pousse les actions à droite), title="..." pour tooltip natif au hover sur ellipsis. Adapté petit écran (min-width auto en ≤900px). Cache-bust 7.65→7.66.
- Booster passe globale OSM — 3 endpoints round-robin (idée Stefan 31/05 PR #711 : « il existe d'autres serveurs, Scout les utilise »). Vérification scout.html : 3 endpoints utilisés (overpass-api.de + kumi.systems + private.coffee), j'en avais 2. Ajout du 3e + refactor en round-robin : compteur _rrIdx module-scope alterne le point de départ de chaque requête (req1→endpoint[0], req2→endpoint[1], req3→endpoint[2], req4→endpoint[0]...), fallback sur les autres si échec. Charge répartie sur 3 serveurs au lieu de saturer overpass-api.de seul. CSP index.html étendue à private.coffee. Test « TOUS échouent » 8→12 calls (4 tentatives × 3 endpoints). Cache-bust 7.63→7.64.
- Fix passe globale OSM — rate limit Overpass massif (incident Stefan PR #710 : 275/297 échecs en batch). Diagnostiqué via snippet console IDB. Cause : concurrency 3 + zéro délai → overpass-api.de rejette la majorité (soft rate limit ~2 req/IP/s). Fix : (a) src/osm-overpass.js : retry automatique avec backoff 1s/3s/8s sur l'ensemble (endpoint principal + fallback), timeout porté à 8s. (b) src/osm-pass.js runInitialBatch : CONCURRENCY 3 → SÉQUENTIEL avec délai 250ms entre requêtes (≈5min pour 297 POI mais one-shot par destination), ré-évalue aussi les status='failed' avec force:true pour ignorer cache obsolète → l'admin peut relancer la passe pour rattraper les échecs précédents. Aucune action manuelle requise pour le rattrapage : un simple « Quitter + relancer » la passe re-tente automatiquement les failed. Pas de cache-bust CSS.
- Fix passe globale OSM (PR #710 retours Stefan en preview localhost) : 2 bugs. (a) Zoom/pan Leaflet bloqué pendant la passe — le ::after de .osm-pass-overlay couvrait la zone carte avec pointer-events:auto pour bloquer les clics POI parasites, mais neutralisait AUSSI scroll/drag. Fix : retrait du ::after. Conséquence assumée : si l'admin clique un marker pendant la passe, openDetailsPanel se déclenche mais rend dans la sidebar HW V2 masquée par body.osm-pass-active → effet visuel nul, la passe continue. (b) Drapeau orange hors viewport sur Rym Beach/Trojette — flyTo(POI, 18) zoomait trop fort et laissait le drapeau OSM (souvent à 50-150m) en dehors. Fix : map.flyToBounds([POI, drapeau], padding généreux + maxZoom 18) qui GARANTIT que les 2 points sont visibles, avec padding asymétrique tenant compte du panneau gauche 340px et de la barre d'action en bas. Fallback flyTo(POI, 18) si pas de drapeau (status='failed' sans suggestion). Cache-bust 7.62→7.63.
- CHANTIER drapeaux d'accès v2 PR 3/5 — Passe globale OSM (admin). Outil de revue exhaustive des POI off-route en une session. (1) Nouveau src/osm-pass.js : startOsmPass entry point, batch Overpass initial concurrency 3 sur status=undefined, navigation next/prev, actions Conserver/Remplacer/Effacer/Suivant, drag drapeau (bascule en moved), modale confirm sortie si modifs non sauvées. Helpers purs shouldIncludeInPass + filterItems exportés pour tests. (2) Nouveau style/osm-pass.css : pattern overlay z-index 1500, body.osm-pass-active masque .right-sidebar le temps de la session, panneau gauche 340px (header + progression + filtres pill + recherche + liste scrollable), barre d'action ancrée en bas (fond sombre hérité v1), bouton Quitter haut-droite, bandeau loading central. (3) src/admin-control-ui.js : nouvelle card « Passe drapeaux OSM » dans Outils dashboard CC + handler async qui closeCCModal puis import('./osm-pass.js').startOsmPass(). (4) src/map.js createHistoryWalkIcon : ajout pastille .hw-poi-flagdot ambre sur le marqueur si state.isAdmin && accessPointStatus='failed' (signal carte pour repérer les POI à reprendre hors passe globale). (5) Tests : 8 tests jsdom (shouldIncludeInPass + filterItems). Cache-bust style.css 7.61→7.62.
- Fix bug barre point d'accès figée au changement de POI (signalé Stefan post #708). La barre .ap-bar (lancée via item kebab fiche POI) ne se fermait pas quand l'admin sélectionnait un autre POI → barre liée au POI A reste visible alors que la fiche affiche POI B + ligne pointillée incohérente. Cause : startAccessPointPlacement teardown au DÉBUT de session mais aucun listener sur le changement de POI courant. Fix : ui-details émet eventBus 'details-panel:opened' {poiId} et 'details-panel:closed' ; access-point-editor s'abonne au top-level (module importé statiquement par ui-details) et appelle teardown() si _poiId actif diffère du nouveau poiId (ou panel fermé). Pas de cache-bust CSS (pas de changement style).
- CHANTIER drapeaux d'accès v2 PR 2/5 — Overpass + pré-pose silencieuse + bouton « Pré-poser via OSM ». (1) Nouveau src/osm-overpass.js : query way[highway](around:150,lat,lon), endpoint principal overpass-api.de + fallback kumi.systems, timeout 5s, retour {coords:[lon,lat], distance:m}. (2) Nouveau src/access-point.js : logique centrale prepoSeAccessPoint(feature, {force}) — cache → Overpass → seuil 10m → écrit accessPoint + accessPointStatus enum (osm/moved/on-track/failed). Helper getAccessPointStatus pur. Constante ACCESS_POINT_THRESHOLD_M=10. (3) src/database.js : DB_VERSION 6→7 + nouveau store osmNearestWay (clé [mapId,poiId]), helpers getCachedNearestWay/setCachedNearestWay, pas de TTL. (4) src/access-point-editor.js : couleur drapeau dynamique (osm=orange si status='osm', sinon moved=vert), drag bascule en moved, nouveau bouton « Pré-poser via OSM » (direct si pas de drapeau OU orange, modale showConfirm si on écrase vert), save écrit accessPointStatus en cohérence. (5) src/richEditor.js executeCreate : schedulePrepoSe fire-and-forget après création POI admin (couvre les 3 chemins ghost qui convergent ici). Toast warning UNIQUEMENT si status='failed'. (6) src/templates.js : chip .poi-tag--access-point en couleur selon status (orange/vert/ambre), label « À évaluer » si failed. (7) style/panels.css : variantes .poi-tag--access-point.is-osm/is-moved/is-failed + pastille .hw-poi-flagdot prête (consommée PR3). (8) Tests : 24 tests (osm-overpass + access-point avec mocks complets jsdom). Cache-bust style.css 7.60→7.61.
- Fix barre .ap-bar primary illisible en night : `html[data-theme="night"] button { color: var(--ink) }` (base.css, specificity 0,1,1) surchargeait `.ap-bar-btn--primary { color: var(--bg) }` (specificity 0,1,0) → texte #F3F4F6 sur fond #fff = blanc-sur-blanc. Fix : préfixer toutes les règles boutons de la barre par `.ap-bar` (specificity 0,2,0) pour battre l'override global. Cache-bust 7.59→7.60.
- CHANTIER drapeaux d'accès v2 PR 1/5 — refonte visuelle (palette --flag-osm/-moved/-locked sur 4 thèmes + état CSS osm/moved/locked/pending/error) + redesign barre unitaire (.ap-bar : pastille icône + titre/consigne + mesure d'écart LIVE en m via Haversine pendant le drag + actions Effacer/Annuler/Enregistrer, fond sombre hérité v1). Migration : ancien CSS .hw-access-flag* + .hw-access-toolbar* SUPPRIMÉ de panels.css ; nouvelle structure .hw-flag + .hw-flag--moved (par défaut faute de champ source — viendra PR2). Icône Lucide `Flag` ajoutée à appIcons + migration des 2 sites lucide map-pin-plus → flag (item kebab fiche POI + chip .poi-tag--access-point). Token --success ajouté au bloc neutre tokens.css (alias intent positif, manquait). Cache-bust style.css 7.58→7.59. Bouton « Pré-poser via OSM » + champ accessPointSource + intégration Overpass = PR2. Pas de logique métier changée : les drapeaux existants se lisent/écrivent pareil, GPX export intouché.
- CHANTIER renommage Description_courte → info_gpx (lowercase). Suite de #704. L'ancien nom partageait son préfixe avec `description` (longue) → confusion sur ce qui est quoi. Sémantique vraie : ce champ est le texte qui voyage dans le <desc> des waypoints du GPX exporté (visible Wikiloc / GPX Studio / applis de marche) → alimente la SORTIE (rectifié par Stefan : pas une entrée importée). `info_gpx` reflète cet usage. Case lowercase = trajectoire #704. Migration : (1) scripts/migrate-info-gpx-rename.mjs sur public/*.geojson (115 renommés : 62 djerba + 53 hammamet, 0 conflit, idempotent). (2) data.js displayGeoJSON : nouveau helper database.renameDescriptionCourteToInfoGpxInPoiData (curseur store poiUserData r/w) + extension du scan en mémoire sur appState.userData + customFeatures (gère Description→description ET Description_courte→info_gpx en une seule passe). Écrivains : DM storage.js, scout.html (2 sites). Lecteurs : gpx.js (export waypoint <desc>), templates.js (section fiche), mobile-follow.js (excerpt bottom sheet), richEditor.js (load+save form, 2 sites). admin-diff-engine ajoute mapping displayKey 'info_gpx'→'Info GPX' pour libellé UI du CC. BONUS fix : main.js du DM lisait p['Description'] capital pour charger le form long-description — oubli de la PR #704, le DM ouvrait des forms vides — corrigé en même temps avec fallback transitoire (cache localStorage DM). Vérifié preview : fiche Sidi Zayed affiche info_gpx, export GPX waypoint <desc> = info_gpx du POI (Rym Beach). 859 tests + knip 0.
- CHANTIER unification clé `description` (lowercase) — dette tech soldée. Historiquement deux cases coexistaient (Description capital pour geojson source + DM, description lowercase pour richEditor) → chaque lecteur faisait un fallback `a || b`, oubli = bug (cf. TTS al-Ahjar PR #703). Désormais une seule clé canonique : `description` lowercase partout. Migration : (1) scripts/migrate-description-case.mjs normalise public/*.geojson, idempotent ; règle de fusion = lowercase prioritaire en cas de conflit (5 cas réels sur djerba, tous résolus en faveur de la version curée la plus récente). (2) data.js displayGeoJSON : migration runtime AVANT lecture du store (normalizeDescriptionCaseInPoiData dans database.js, curseur sur store poiUserData en transaction r/w) + passe en mémoire sur appState.userData + state.customFeatures, persistées si touchées. Écrivains migrés : DM storage.js + tools/scout.html (3 sites). Lecteurs simplifiés en `.description` direct : data.hasDescription, templates.buildDetailsPanelHtml, mobile-follow.sheetMarkup, ui-details.speakText, richEditor (loadForEdit). admin-diff-engine garde le mapping rawKey='description' → displayKey='Description' pour le libellé UI. Tests : admin_geojson assertions migrées en lowercase, data_module.it.each retiré le cas Description capital (case morte). Vérifié preview : TTS + fiche OK sur POI lowercase (al-Ahjar) et POI ex-capital migré (Phare de Taguermess). Followup parqué : renommer Description_courte en Info_gpx ([[project_description_courte_rename]]).
- Fix TTS « Pas de description » sur les POI récents. Le handler speak ([ui-details.js]) cherchait userData.description || props.Description || userData.Description — il OUBLIAIT props.description (lowercase). Or le richEditor (et le DM) enregistrent toujours la description longue sous `description` lowercase ([richEditor.js:649,810]). Conséquence : tout POI créé après bascule rich editor / DM avait sa description visible sur la fiche (templates.js spread les 2 cases) mais le TTS disait « Pas de description » (cas observé par Stefan sur Mosquée al-Ahjar). Fix : aligné sur la logique d'affichage ({...props, ...userData}.description || .Description) → TTS lit ce que la fiche montre. Vérifié preview : speakText reçoit la vraie description sur al-Ahjar.
- CC Admin — Publication : (1) FIX la modale CC ne se fermait JAMAIS après publication réussie. publishChanges appelait closeModal() (modal.js) qui ne sait fermer que les modales legacy/V2, mais le CC a son propre shell .admin-cc-overlay depuis la refonte PR3 → no-op silencieux. Bascule sur closeCCModal() (admin-control-ui.js, fermeture dédiée qui révoque aussi les blob URLs). (2) FEATURE progression dans le bouton « TOUT PUBLIER » pendant l'envoi (au lieu d'« Envoi… » statique) : "Photos N/M" (compteur live sur l'upload séquentiel), "Géojson…", "Statut vérifié…", "Circuits N/M", "Nettoyage…" puis ferme. Helper setBtnLabel inline (spinner loader-2 + libellé court). Pas de nouvelle surface (réutilise le bouton existant). Le toast de récap (« N publié(s)… ») inchangé.
- Popup « Nouveau Lieu ? » — fix contraste mode nuit : pastilles Maps/OSM utilisaient --brand (#60A5FA en nuit, bleu clair pensé pour fond sombre), or le popup Leaflet a un fond BLANC fixe (pas thémé) → texte/bordure quasi invisibles en nuit (signalé par Stefan). Bascule sur --cta-bg (toujours foncé dans les 4 thèmes : navy/brun/vert/navy) pour color+border. Hover passé à rgba(0,0,0,.05) (gris neutre) au lieu de --surface-muted (qui devient sombre en nuit, et incohérent sur le popup blanc). Cache-bust 7.56→7.57.
- Popup « Nouveau Lieu ? » — fix : icônes Lucide + centrage (PR précédente publiée non finie). Les <i data-lucide="map-pin"/"map"> n'étaient pas convertis en SVG (aucun appel createIcons sur ce DOM dynamique) → boîte vide 12px à gauche du texte → texte décalé à droite (signalé par Stefan). Fix : createIcons({icons:appIcons,root:popupContent}) AVANT bindPopup dans les 2 popups jumelles (desktopMode + searchManager). Filet CSS : justify-content:center sur .ghost-popup-link + min-width:72px pour pastilles équilibrées et texte centré quelle que soit la longueur de l'étiquette. Cache-bust 7.55→7.56.
- Popup « Nouveau Lieu ? » — boutons Maps/OSM remplacent la ligne de coords texte. Avant : il fallait sélectionner-copier les coords pour aller les vérifier. Désormais 1 clic ouvre l'onglet (pastilles .ghost-popup-link, icônes map-pin/map, label brand). Touche les 2 popups jumelles : clic droit carte (desktopMode.createDraftMarker) ET recherche par coords (searchManager). Les boutons lisent marker.getLatLng() à la volée → reste juste après drag, plus de re-render texte. CSS classe .ghost-popup-coords supprimée du partial (devenue morte). Cache-bust style.css?v 7.54→7.55. Réutilise openCoordsOnMap (utils.js, ajoutée pour la PR import photo orpheline).
- Import photos — Maps/OSM sur groupes orphelins / Hors POI (ouvre le BARYCENTRE EXIF des photos, pas un POI). Rend visible « où l'app a positionné cette photo » pour diagnostiquer un cluster aberrant (ex. orphelin proposé à un POI 1,8 km loin alors que l'EXIF colle à 8 m d'un autre). Nouveau helper utils.openCoordsOnMap(lat,lng,provider) factorisé depuis openPoiOnMap (qui le délègue). ui-photo-batch buildClusterActions : le bloc Maps/OSM existant (gaté sur hasNearbyPoi) couvre désormais aussi !hasNearbyPoi && !noGps via getClusterCenter ; tooltips contextuels (« Voir la position de la photo » vs « Vérifier ce lieu »). Sans GPS reste sans boutons (pas de coords). Aucun changement de URL/markup côté rattaché.
- Import photos — « Enregistrer ce lieu » par groupe (save d'un seul POI sans fermer la modale, pour trier lieu par lieu). Logique de save factorisée : saveCluster(cluster,mapId) = source unique (compress+watermark admin+merge dédup+split admin/user+flag alreadySaved), réutilisée par handleSave (footer, sauve tout puis ferme) ET handleSaveCluster (par groupe, ne ferme pas → toast + re-render). Bouton affiché sur un groupe rattaché non-savedAsNewPoi ayant des photos non sauvées ; une fois tout sauvé il passe à l'état vert inerte « Enregistré » (.pb-act.is-saved, --ok-soft/--ok-fg). Anti double-clic (disabled au clic, restauré par renderBody si erreur). Cache-bust style.css?v 7.53→7.54.
- Import photos — photos SANS GPS gérées (feature user : images du net = quasi jamais d'EXIF GPS). Avant : `withGps.filter` les jetait en silence + early-return « Aucune coordonnée GPS » si 100% sans GPS. Désormais : dédup hash sur TOUTES les photos, split gpsItems/noGpsItems ; les sans-GPS forment UN groupe « Sans GPS » poussé EN BAS de la liste (center:null, noGps:true) + toast récap (« N photo(s) sans GPS… »). Dans la modale : badge « Sans GPS », sous-titre « À rattacher à un lieu », bouton « Rattacher à un lieu… » (icône link) — PAS « Créer un lieu » (pas de position carte) ni « plus proche » (pas de distance). Le bouton ouvre un picker de POI recherchable (openPoiPickerModal, réutilise getSearchResults + les classes .sp-* de start-point/modals.css → 0 CSS neuf) via suspend/resumeHwModal (même garde anti-empilement que « Créer un lieu ») ; au choix, attachClusterToPoi(cluster,{feature,dist:0}) bascule le groupe en POI sauvegardable (noGps:false). MVP = rattachement du groupe entier à 1 lieu (cas dominant : on télécharge les photos d'UN lieu) ; répartition multi-lieux non incluse (parquée). Pas de cache-bust CSS (réutilise .sp-*).
- Import photos — boutons Maps/OSM du groupe alignés sur la mini-barre fiche Lieu (« de vrais boutons », demande Stefan) : classe .pb-act.is-maplink avec label « Maps » / « OSM » (au lieu de l'icône grise seule .is-icon) + icône bleue (--brand) + hover bordure/texte bleus, comme .poi-quick-btn. Hauteur 32px conservée (cohérence avec sélecteur/Comparer/ZIP de la même barre, tous à 32px — pas le 44px de la fiche, qui aurait dépassé). Comportement (par coordonnées, gaté si POI rattaché) inchangé. Cache-bust style.css?v 7.52→7.53.
- Import photos — boutons « Vérifier sur Maps / OSM » par groupe + harmonisation libellés fiche. Nouveau helper utils.openPoiOnMap(feature,'gmaps'|'osm') ouvrant la position d'un POI PAR COORDONNÉES (jamais par nom : nom faux → recherche vide/mauvais lieu). Réutilisé : (1) fiche POI mini-bar (ui-details : 2 handlers refactorés sur le helper, suppression duplication window.open) ; (2) import photo (ui-photo-batch buildClusterActions : 2 boutons icône map-pin/map sur un groupe RATTACHÉ, avant « Comparer », ouvrant les coords du POI suggéré pour vérifier/corriger son nom ; masqués si pas de POI rattaché). Libellé fiche harmonisé : « Vérifier sur Google Maps » → « Voir sur Google Maps » (même verbe que « Voir sur OpenStreetMap »). Pas de CSS (réutilise .pb-act.is-icon). Google par nom (btn-web-search) écarté. Vérifié preview : boutons gatés (présents si POI rattaché), URLs par coords. 860 tests + knip 0.
- Import photos — déduplication par HASH (dédup 2-local). Nouveau helper utils.sha256OfFile(file) (SHA-256 du fichier ORIGINAL, jamais du blob compressé). À l'import (desktopMode.handleDesktopPhotoImport, ETAPE 1.5), on calcule le srcHash de chaque photo et on ÉCARTE pour de bon (a) les doublons intra-import (Set seenThisImport) et (b) ceux dont le hash est déjà stocké en local (getAllPoiPhotoHashes user / getAllPendingAdminPhotoHashes admin — nouveaux getters database.js via index mapId_index). Toast récap « N doublon(s) ignoré(s) » (option a, silencieux à l'unité). srcHash persisté sur chaque photo sauvée dans les 3 chemins (ui-photo-batch handleSave, desktopMode addPhotosToPoi, ui-photo-grid handleSave) → { id, blob, srcHash }. addPhotosToPoi : ancienne dédup approximative par TAILLE de blob remplacée par le srcHash. NE couvre PAS les photos déjà publiées sur GitHub (hash absent en local après vidage de pendingAdminPhotos à la publication) — limite assumée, option 2-complet (srcHash dans properties.photos du geojson) PARQUÉE. +tests/sha256_of_file.test.js (5 cas). Vérifié preview : boot OK, round-trip IDB (store→read srcHash via getAllPoiPhotoHashes), blocksSame/allowsDiff OK, 0 erreur. 860 tests verts, knip 0.
- Mode Comparer — fix régression grille + pellicule toujours visible. (a) BUG de ma PR précédente : l'Edit qui posait --pb-cols/--pb-rows dans renderFocus avait échoué silencieusement → la grille restait sans variables de rangées → sur 3440 les 5 cellules tombaient en 1 rangée étirée → photos coupées verticalement. Corrigé : compare.style.setProperty('--pb-cols'/'--pb-rows', gridForSlots(slotCount)). (b) Pellicule (bande de vignettes) repoussée hors écran (il fallait scroller) : la chaîne flex de focus n'était pas height-bounded (hw-modal-body display:block → pb-body flex:1 inerte ; + règle .is-focus overflow:hidden à 3 classes battue par la règle plein écran à 4 classes). Fix CSS : .is-photo-batch.is-focus .hw-modal.is-xl .hw-modal-body devient flex column overflow:hidden (5 classes, gagne) + .pb-cluster.is-focused devient flex column → le groupe focalisé tient EXACTEMENT dans la hauteur modale, la grille est bornée par la hauteur (photos entières) et la pellicule flex-shrink:0 reste toujours visible SANS scroll (réponse au point bonus Stefan, pellicule gardée en bas plutôt que déplacée dans le bandeau). Cache-bust style.css?v 7.51→7.52. Vérifié preview 3440×1440 : 5 cellules 3×2, 0 débordement, pellicule visible sans scroll.
- Mode Comparer (import photos) — taille sur grand écran + emplacements remplis. (1) Fix « on ne voit que 50% de la photo » sur écran large : la grille de comparaison passe en rangées CSS `1fr` (repeat(var(--pb-rows),1fr)) au lieu de rangées `auto` → chaque photo est désormais bornée par la HAUTEUR dispo (avant, la rangée auto prenait la hauteur naturelle de l'image → débordement). cols/rows pilotés par variables CSS posées par renderFocus (gridForSlots : ≤4 = 1 rangée, 5-6 = 3×2). (2) Défaut adaptatif à l'entrée du focus (defaultCompareSlots) : on affiche ce que la largeur permet (cellule ~600px), borné par MAX_COMPARE_SLOTS=6 et le nb de photos — demi-écran≈2, 3440 plein écran≈5 (retour Stefan : garder la taille de photo + plus de photos côte à côte). (3) Sélecteur 2→6 (au lieu de 2/3/4), masqué si ≤2 photos, plafonné au nb de photos. (4) Sous-bug corrigé : cliquer 3/4/5/6 remplit les nouveaux emplacements avec des photos non affichées (fillEmptySlots) au lieu de laisser des cases vides (setSlotCount + focusPelliculeTap plafond 4→6). Cache-bust style.css?v 7.50→7.51.
- Import photos — groupement « par lieu » (phase enrichissement). Nouveau module src/photo-clustering.js (fonction PURE, testée) : chaque photo rattachée au POI le plus proche (≤120m) → 1 groupe = 1 POI ; le POI d'ancrage forcé en tête de nearbyPois (= nom + cible save) ; photos sans POI proche → groupes « trajet » regroupés par proximité (clusterByLocation 80m). Corrige le chaînage en ville de l'ancien clustering transitif photo↔photo (rafale de photos de plusieurs lieux → méga-cluster mal nommé, ex. « Café thé Baklawa · 26 photos » mélangeant église/poste/mosquées). desktopMode.handleDesktopPhotoImport route l'import via clusterPhotos(validItems, loadedFeatures, hiddenPoiIds) ; ancien pipeline inline (clusterByLocation/filterOutliers/enrichissement/mergeClustersBySamePoi) supprimé. filterOutliers (utils.js) supprimé (devenu mort). Méthode validée par Stefan sur ses vraies photos (by-poi=12 groupes vs proximity=7) ; la méthode « proximité » concurrente + son switch de comparaison admin (provisoires) ont été retirés après ce choix (replace=cleanup — main n'a jamais vu le switch). +tests/photo_clustering.test.js (8 cas : split lieux proches, ancre en tête, trajet, désordre, hiddenPoiIds, tri, vides).
- Suivi de circuit sur mobile — PR4 (peaufinage Lot 1, retours test mobile Stefan) : 3 fixes CSS sur l'écran de suivi. (1) Les contrôles carte (zoom/recentrer) chevauchaient le bouton « Autoriser » de l'invite GPS refusée → masqués via une classe .has-invite (togglée dans setGpsState quand stateName==='denied'), même logique que .has-sheet (pas de point bleu à recentrer ni zoom utile dans cet état transitoire). (2) Nom du circuit tronqué sur 1 ligne → -webkit-line-clamp 2 (2 lignes max puis ellipsis ; eyebrow « SUIVI · zone » conservé car discret et situant). (3) Boutons du bottom sheet déséquilibrés (« Marquer visité » flex:1 mangeait tout, « Fiche » réduit au contenu) → ratio ~60/40 (flex 3 / flex 2) : « visité » reste primaire (plus large) mais « Fiche » devient flexible. POI hors tracé = VOULU (sentier non cartographié, cf. accessPoint), ne pas toucher. Cache-bust style.css?v 7.48→7.49.
- Suivi de circuit sur mobile — PR3 (POI cliquables + bottom sheet aperçu, chantier handoff Claude Design) : le suivi immersif (PR1+PR2) gagne les marqueurs POI du circuit CLIQUABLES (L.layerGroup avec createHistoryWalkIcon, même icône ocre que la carte PC) et un bottom sheet d'aperçu mi-hauteur (variante A : photo à gauche + ÉTAPE n/total · catégorie + nom + extrait Description_courte) avec toggle « visité » (updatePoiData(poiId,'vu',bool) → vuManual + recomputeVu ; feedback in-place, PAS d'auto-fermeture ni d'auto-avance ; le lieu reste sur la carte, masquage = Lot 2) et lien « Fiche » (quitte le suivi via stopFollow puis openDetailsPanel, car la fiche détaillée se rend dans #mobile-container masqué pendant le suivi). Marqueur visité = atténué + pastille verte de coche (.hw-mk-done, même langage que .cc-step.is-done). Photo : 1re photo publiée (properties.photos[0]) sinon blob local async (getPoiPhotos, objectURL révoqué). Historique : le sheet pousse une entrée ; le Back ferme le sheet AVANT de quitter le suivi (followBack remplace stopFollow dans onHwBack). Le point bleu (zIndexOffset 1000) reste au-dessus des POI ; le puck non-interactif laisse les POI cliquables. Rafts de contrôles masqués quand le sheet est ouvert (.has-sheet). Cache-bust style.css?v 7.47→7.48.
- Suivi de circuit sur mobile — PR2 (point bleu GPS, chantier handoff Claude Design) : le suivi immersif (PR1) gagne le point bleu temps réel (watchPosition → marqueur L.divIcon cobalt #1A73E8 + halo/pulse, hors-thème, anti bleu-sur-bleu vs tracé --brand), un bouton « recentrer sur moi » (raft cobalt sous le pouce, recentre à la demande ; recentrage auto UNE fois au 1er fix puis la carte ne bouge plus seule), le Screen Wake Lock (écran maintenu allumé en marchant, puce discrète dans le header, ré-acquis au retour au 1er plan via visibilitychange, relâché à la sortie) et les états GPS : acquisition (toast spinner « Recherche du signal GPS… ») et refusé/indisponible (invite « Localisation désactivée » + bouton Autoriser qui relance watchPosition ; le tracé reste affiché). watchPosition démarré dans startFollow, clearWatch dans stopFollow ; point bleu = L.marker non-interactif zIndexOffset 1000, retiré au teardown. Écran éteint/arrière-plan = hors scope (limite PWA : watchPosition gelé). PAS de turn-by-turn/routing. Icône Sun ajoutée à appIcons. Cache-bust style.css?v 7.45→7.46.
- Suivi de circuit sur mobile — PR1 (mode immersif, chantier handoff Claude Design) : nouveau CTA « Suivre ce circuit » sous la description de la fiche circuit mobile (mobile-poi), affiché SEULEMENT si le circuit a une vraie trace (circuitHasTrace : fichier .gpx officiel OU realTrack peuplé ; sinon note « Pas de trace GPS — suivi indisponible »). Au clic, startFollow (nouveau src/mobile-follow.js) RÉVÈLE la carte Leaflet existante (masquée display:none sur mobile) en plein écran position:fixed z-index 1100 via body.follow-active (style/mobile-follow.css), masque #mobile-container + les contrôles Leaflet par défaut, pose un header overlay minimal (eyebrow zone + titre tronqué au « via » + croix) + contrôles zoom +/−, puis invalidateSize + cadrage (fit-bounds) sur le tracé réel déjà dessiné par notifyCircuitChanged. La croix fait history.back() ; le Back Android et la croix passent par onHwBack qui détecte isFollowActive() → stopFollow (teardown overlay + re-masque la carte). PAS de GPS/point bleu (PR2) ni bottom sheet POI (PR3) à ce stade. Icône Navigation ajoutée à appIcons (lucide-icons). Cache-bust style.css?v 7.44→7.45 (nouveau partiel @importé). Vérifié en preview mobile 412×892 : CTA gaté (présent sur circuit à trace, absent sinon), révélation plein écran + tracé Voyager + header/croix/zoom, round-trip fiche→suivi→fiche, 0 erreur console.
- Bouton plein écran dans la topbar PC (Fullscreen API) : à côté du bouton Thème, icône maximize ↔ minimize qui se resynchronise via fullscreenchange (donc cohérente même si l'utilisateur sort via Échap ou F11). Discoverable sans connaître F11, sortie explicite par clic. setupFullscreenToggle() câblé dans setupDesktopUIListeners ; nouveau module src/fullscreen-toggle.js (~50 lignes) ; icônes Minimize/Minimize2 ajoutées à appIcons. Masqué si le navigateur ne supporte pas requestFullscreen (rare). PC-only (la PWA mobile est déjà plein écran display:standalone).
- Harmonisation PC — Direction A (handoff Claude Design) : topbar + 4 contrôles carte parlent désormais le même langage doux que la sidebar droite. (1) Topbar : remplacement des rgba(255,255,255,...) hardcodés par les tokens --on-brand-* (déjà présents dans tokens.css) sur les boutons .topbar .btn (fond/bordure/hover), l'input #search-input et le séparateur ; suppression du translateY(-1px) au hover (la sidebar n'en a pas) ; radius 12px isolé → --radius-md (10px) sur .topbar .btn, #search-input et .hw-dest-selector. État actif .topbar .btn.active passé de --cta-bg à --brand (plus doux). (2) 4 contrôles carte Leaflet (zoom +/−, layers, recentrer, légende) refondus en RAFT de cartes claires : background --bg navy plein + --shadow lourde + bordure --line tranchée + translateY hover → --surface + --shadow-soft + radius --radius-lg sur le conteneur .leaflet-bar + .leaflet-control-layers, séparateurs internes fins --line entre boutons groupés, hover --surface-muted, icône layers en mask currentColor (suit --ink-soft / --ink au hover) au lieu de blanc. Espacement vertical inter-groupes 10px. (3) Légende DÉPLACÉE de la topbar vers le raft : #btn-legend retiré d'index.html, ajout d'un L.Control custom LegendControl dans map.js (link id=leaflet-btn-legend, icône book-open) qui appelle showInfoPopover() ; info-popover.js fait un fallback de lookup d'ancre (leaflet-btn-legend || btn-legend) qui n'affecte pas les tests existants ; listener mort retiré d'events-desktop.js, import inutile retiré, 'btn-legend' retiré de la liste DOM d'ui.js. ResetViewControl perd ses styles inline (var(--bg) + #fff) → hérite du raft. Vérifié en preview les 4 thèmes maritime/desert/oasis/night : raft cohérent, popover Légende s'ouvre depuis le nouveau bouton. Hors scope strict : sidebar droite, mobile, marqueurs POI/clusters, viewer photo, modales.
- Splash de boot : mascotte furet (icône maskable) + spinner brun sur fond crème #F9E2B8, à la place de la photo de mosquée (splash-bg.webp, typée Djerba → incohérente en multi-destinations) ; image supprimée + préchargement retiré.
- Branding : icône furet (mascotte Heripia) — icon-192/512 régénérées depuis le portrait + versions maskable ajoutées (manifest purpose:maskable) ; image OG DÉDIÉE 1200×630 (public/og-image.png, furet + slogan « Marchez dans l'histoire ») remplaçant l'icône carrée pour og:image/twitter:image ; twitter:card summary→summary_large_image (vraie bannière) + og:image:width/height/alt ; favicon cache-busté (?v=2).
- Découvrabilité / classification web : métadonnées descriptives ajoutées au <head> (index.html) — <title> enrichi, meta description, Open Graph + Twitter card, JSON-LD WebSite — et public/manifest.json gagne `description` + `categories:["travel","education","navigation"]`. But : donner aux moteurs de catégorisation (proxies d'entreprise type Zscaler/Fortinet…) de quoi classer Heripia en patrimoine/voyage plutôt qu'« Entertainment/Games » (faute de tout signal sémantique aujourd'hui : title nu, 0 description, 0 OG). Lu même sous noindex (le noindex ne vise que les moteurs de recherche, pas les classifieurs de sécurité qui crawlent l'URL). <meta noindex> CONSERVÉ (retrait au lancement public). Wording title/description = placeholder à valider par Stefan. og:image = icône 512 (une vraie image 1200×630 serait mieux, à fournir).
- Sécurité (défense en profondeur) : sanitizeHTML (utils.js) retire désormais TOUS les attributs d'événement inline (on*, ex. onerror/onclick/onload) sur n'importe quel élément, en plus des <script> et des href javascript: déjà nettoyés. Réconcilie le code avec sa docstring (qui annonçait déjà ce nettoyage) et retire le commentaire obsolète « l'app s'appuie sur des onClick inline » — vérifié : 0 handler inline dans tout le repo (addEventListener only) et la CSP stricte sans 'unsafe-inline' les bloquait déjà → 2e barrière (reco audit externe). +tests/sanitize_html.test.js (jsdom).
- Cohérence manifest/Vite : public/manifest.json `scope`/`start_url` passent de './' (relatif) à '/' (absolu, racine), alignés sur la base Vite '/' (cutover heripia.com). Comportement identique sur le domaine racine ; lève l'ambiguïté relevée à l'audit externe (install/launch sur URL profonde, app multi-pages). JSON statique, aucun autre changement de code.
- Tri mobile aligné sur PC (Lot 2 parité) : ordre Proximité / Court / Long / Vérifiés (Vérifiés en dernier, comme PC) + libellés « Court » / « Long » au lieu de « Du plus court / long ». Markup #ms-sort-grid (mobile-circuits) uniquement ; aucun changement de logique de tri.
- Parité fiche circuit mobile/PC (Lot 1) : le bouton « Cacher / Réafficher ce circuit » passe de la carte (Phase 1c #669) à la barre d'actions de la fiche circuit mobile (.cc-actions, à côté de « Fait »), comme sur PC. Retiré de la carte (.mc-hide + handler + imports + CSS). setCircuitHidden inchangé ; mobile-poi appelle le helper. Cache-bust style.css (mobile.css : .cc-act.is-hidden-circuit).
- Phase 1c mobile (Mon Espace V2) : « Cacher / Réafficher ce circuit » sur mobile (bouton œil eye-off/eye sur la carte .mc-card, à côté du toggle « fait ») + filtre « Visibilité » (Visibles/Cachés/Tout) dans la sheet de filtres mobile (réplique PC). La mutation blacklist hiddenCircuitIds est factorisée dans circuit-actions.js (setCircuitHidden : set + saveAppState + schedulePush + eventBus + applyFilters) ; le handler PC (ui-circuit-page-events) consomme désormais ce helper partagé (imports morts nettoyés). Cache-bust style.css (mobile.css : .mc-hide).
- Volet 2 « point de départ » : le réglage du tri « Proximité » (retiré de Mon Espace par #640) revient via une modale dédiée (start-point.js) — soit la position GPS, soit le choix d'un lieu de l'app — ouverte au clic sur « Proximité » sans point défini (PC ui-circuit-list + mobile mobile-circuits), avec une ligne « Depuis <lieu> · modifier » sous le tri. Remplace le toast « définis ton lieu dans Mon Espace » devenu un cul-de-sac (le réglage n'y était plus). Persistance saveAppState('homeLocation') inchangée ; champ label ajouté. Cache-bust style.css (modals.css : classes .sp-*).
- CUTOVER vers heripia.com : base Vite '/History-Walk-V1/' → '/' (racine du domaine custom) — vite.config (base + PWA + scope), image du splash (style.css), chemins DM↔HW (admin-control-ui + datamanager/main.js), playwright. public/CNAME = heripia.com (active le domaine au déploiement). URLs des GPX (<link> + generate-circuit-index) → https://heripia.com/. Signature/desc/index/fallback enrichis en « Circuit généré par Heripia — heripia.com » (pub dans les fichiers partagés, visible Wikiloc). public/robots.txt + <meta noindex> = TEMPORAIRES (phase peaufinage) à RETIRER au lancement public. README : URL de prod → heripia.com. ATTENTION : l'URL …github.io/History-Walk-V1/ cesse de fonctionner (basculement attendu).
- Signature de circuit harmonisée → « Circuit généré par Heripia. » (au lieu de « (Créé par Heripia) ») : même formulation que le <desc> GPX / l'index / le fallback, et valorise mieux la génération de circuits (demande Stefan 25/05). Garde-fou anti-doublon inchangé (la signature contient toujours « Heripia »).
- Marques sur contenu généré → « Heripia » (FUTURS exports uniquement) : <desc>/<text>/creator des GPX (préfixe technique [HW-ID:] conservé), signature de circuit « (Créé par Heripia) » avec garde-fou anti-doublon (le check reconnaît « History Walk » ET « Heripia » → un circuit déjà signé n'est jamais re-signé), constante de description d'index (buildCircuitIndexEntry), fallback d'affichage (mobile-poi), filigrane photo « © Stefan Martin — Heripia ». Aucune réécriture rétroactive (GPX/circuits/photos existants intacts ; filigrane appliqué à l'upload). URLs des GPX laissées pour le cutover domaine. Tests circuit-index-entry + photo_service adaptés.
- Rebrand « History Walk » → « Heripia » : tous les noms VISIBLES (titre d'onglet fixe + 2 titres dynamiques, modale de bienvenue, mentions légales, footer menu mobile, hint « équipe Heripia » PC+mobile, bandeau d'installation, overline du sélecteur de destination, manifest name/short_name, objets des e-mails contact/signalement, titres Scout + Data Manager + notif brouillon DM, en-tête style.css) + icônes 192/512 (nouveau visuel furet, généré depuis source 1024×1024). Identifiants INTERNES inchangés (HistoryWalkDB, préfixe HW-ID, nom du repo, base path, package name, adresse e-mail de contact). Signatures GPX + filigrane photo + descriptions par défaut réservés à une PR ultérieure (futurs exports uniquement).
- Design : bouton « Soutenir » (#btn-bmc-topbar + #btn-contrib-bmc) adouci — fond jaune vif #FEF08A → --hw-amber-soft (tinte theme-aware), texte/bordure/icône cœur → --hw-amber (doré, discret). Corrige aussi sa casse en thème nuit (couleurs étaient hardcodées, invariantes). Cache-bust style.css?v 7.37→7.38.
- Fix : watermark admin appliqué au fallback base64 de « Créer un lieu » (desktopMode.addPhotosToPoi) — ce chemin rare (File source absent) sortait un Blob NON watermarké, contrairement au chemin principal compressImage ; en admin on y repasse désormais.
- Fix : retrait du placeholder « Description » injecté en dur à la création de POI (richEditor « Ajouté via Rich Editor » + mobile-nav « Créé sur le terrain ») — il s'affichait comme description sur la fiche quand aucune description longue n'était saisie (templates lit `description || Description`). N'affecte que les futurs POIs ; les POIs déjà créés gardent le texte jusqu'à ré-édition.
- Mode COMPARER refondu (handoff Claude Design) : l'ancienne lightbox plein écran NOIRE est remplacée par un mode FOCUS in-place dans la modale « Traitement photos » (classe .is-focus sur l'overlay). Clic « Comparer » sur un groupe → les autres groupes sont masqués et celui-ci passe en grand : 2 emplacements côte à côte par défaut (sélecteur 2/3/4 dans le bandeau), photos en object-fit:contain (portrait/paysage sans crop ni noir, bandes surface-muted), pellicule CLAIRE harmonisée (vignette in-slot = bord CTA + n° de slot), emplacement ACTIF ; taper une vignette non affichée l'AJOUTE (agrandit jusqu'à 4 emplacements), au-delà remplace l'emplacement actif, actions par photo (détacher/supprimer), retour « Vue d'ensemble » + Échap. Pré-sélection multi-photos SUPPRIMÉE (pastilles .pb-pick retirées de la grille → vue d'ensemble épurée). Header bascule overview ↔ focus (titre « Comparer · <lieu> », icône layout-grid, hint). Photos compare = objectURL pleine qualité (révoqués à la sortie/au re-render) ; pellicule = base64 320px. Tokens HW theme-aware UNIQUEMENT, ZÉRO ambre — tout sur surface/tokens, plus AUCUN overlay noir (4 thèmes OK, night vérifié en preview). openLightbox + closeLightbox + CSS .photo-batch-lightbox-* SUPPRIMÉS. Logique métier inchangée (clustering, Sortable, save, ZIP, créer/rattacher). Vérifié en preview : overview (sans pastilles) → Comparer → focus 2/3/4 → tap pellicule → retour, night sans ambre. Cache-bust : style.css?v 7.36→7.37.
- Refonte PLEIN ÉCRAN de la modale « Traitement photos » (import GPS) — handoff Claude Design. Classe dédiée .is-photo-batch posée sur l'overlay après openHwModal (comme .is-photo-viewer, FIGÉ/non touché) : modale 100vw/100vh, header + footer fonctionnels CONSERVÉS, libère le plafond max-height:70vh du .hw-modal-body. Vignettes 220px (~6 colonnes vs ~10 à 120px), markup .pb-* (cluster sticky head + sous-titre meta à dots, badge Orphelin/Hors POI, grille, label éditable). Affordance de sélection refondue : pastille-coche PERMANENTE (.pb-pick) au lieu du toggle invisible. Ambiguïté ZIP levée : bouton par groupe + footer passent en icône download (= disque) ; « Enregistrer » prend cloud-upload (= dans l'app). Compteurs/pill « N sélectionnée(s) »/hint injectés dans le header. Tokens HW theme-aware UNIQUEMENT, ZÉRO ambre (le badge Orphelin utilise le token sémantique --warn, pas le --hw-amber décoratif) ; ambre résiduel de la lightbox compare détoxifié (--brand). Logique métier inchangée (Sortable, lightbox Voir/Comparer, save, ZIP, créer/rattacher un lieu). Entrée menu « Importer des photos GPS » → « Importer des photos » (plus parlant pour un futur user). Vérifié en preview : plein écran 1280×720, grille 5 col à ~225px, header sticky, pill sélection, 4 thèmes OK (night : surface #1F2937 / ink #F3F4F6). Cache-bust : style.css?v 7.35→7.36 (le SW workbox révise photo-batch.css par son contenu ; CSS partiel = pas de HMR en dev). Itérations post-handoff : (a) vraie PLEINE HAUTEUR — le plafond `.hw-modal.is-xl .hw-modal-body { max-height:75vh }` de photo-grid.css (3 classes, global aux modales xl) bridait le body → bande vide en bas ; battu via 4 classes `.is-photo-batch .hw-modal.is-xl .hw-modal-body { max-height:none }`. (b) PLEINE LARGEUR — retrait du cap `max-width:1680px` sur `.pb-body` → la grille auto-fill adapte le nombre de colonnes à l'écran (ultrawide inclus, demandé par Stefan). (c) Header de groupe NON-sticky + opaque — le sticky translucide (backdrop-blur) laissait transparaître les photos au scroll (« bandeaux ») et 2 headers se chevauchaient entre groupes. (d) Miniatures de grille 200→320px (nettes à ~225px d'affichage sans alourdir l'import).
- Fix « Mes Circuits VIDE » au lancement profond : loadOfficialCircuits (app-startup) chargeait via un chemin RELATIF `./circuits/${mapId}.json`. Quand la PWA s'ouvre à une URL profonde (scan d'un QR de partage …/circuits/x.gpx qui ouvre l'app), ce relatif se résolvait en …/circuits/circuits/djerba.json → 404 (ou index.html via SPA fallback) → 0 circuit officiel chargé → liste vide NON rafraîchissable. Fix : import.meta.env.BASE_URL (chemin fixe) comme les autres loaders (destinations/catégories/geojson). Vérifié en preview : chemin absolu → 24 circuits ; relatif doublé → HTML.
- Service worker — sortie des .gpx du navigateFallback (navigateFallbackDenylist dans vite.config). Sans ça, scanner le QR d'un circuit officiel (qui pointe vers …/circuits/x.gpx) faisait servir l'app (index.html) par le SW au lieu du fichier → HW s'ouvrait sur « Mes Circuits » VIDE. Désormais le .gpx passe au réseau. Étape 1 du chantier « partage de circuit » : permet de tester si GitHub Pages sert le .gpx de façon à l'ouvrir dans une appli de marche (point décisif). NÉCESSITE rebuild + redéploiement + MAJ du SW sur l'appareil pour prendre effet.
- Modale Partager (QR) épurée : retrait du lien « Télécharger le circuit » (le download existe déjà dans la barre d'outils du circuit), du bouton « Fermer » (la croix suffit) et du titre + texte d'aide « Partager le circuit ». Header réduit à la croix seule (règle CSS .hw-modal-header:has(.hw-modal-title:empty)), QR + nom du circuit centrés dessous. PC et mobile unifiés visuellement. Payloads QR INCHANGÉS (mobile = lien d'import ?import= ; PC officiel = lien vers le GPX). Nettoyage CSS mort (circuit-share-hint / circuit-gpx-container / btn-gpx-download).
- Éditeur POI (richEditor) sur mobile : repli des paires en 1 colonne (<600px). La grille 1fr 1fr débordait (~43px) car les <input>/<select> gardent leur min-width intrinsèque (min-width:auto) et refusent de rétrécir. Le repli aligne aussi toutes les rangées sur la pleine largeur → corrige au passage la mésalignement Sous-type/État (121px) vs Catégorie/Zone (131px) et le champ Facebook seul en demi-largeur. 2 colonnes conservées sur desktop. Suit le standard formulaire mobile (1 colonne).
- Peaufinage petites modales (.hw-modal.is-sm = confirm/alert/prompt) : corps aligné à GAUCHE (suppression du text-align:center hérité) au lieu de centré — le multi-ligne centré « en drapeau » devenait illisible et titre/corps/boutons cumulaient 3 alignements différents. + text-wrap:balance sur .hw-modal-title (fini les titres longs au « ? » orphelin, ex. « Réaligner les étapes sur le tracé ? »). S'applique à TOUTES les petites modales partout (HW/DM/Scout/CC Admin) ; le viewer photo (is-xl) n'est pas concerné. Validé desktop + mobile.
- Viewer photo — fin du vide sous le viewer : le .hw-modal-body de base plafonnait à max-height:70vh (60vh mobile), ce qui bridait le viewer à ~70 % de hauteur (la bande de vignettes flottait au-dessus d'un grand vide noir). Fix : max-height:none en mode photo-viewer → la zone photo + la bande remplissent l'écran, et le plein écran prend la VRAIE pleine hauteur. Complète la bande séparée (3.7.90).
- Viewer photo — refonte immersive du chrome (handoff Claude Design) : fond NOIR opaque (plus de transparence latérale), header hw-modal MASQUÉ → image bord à bord, compteur « Photo X/Y » RETIRÉ (la pellicule suffit). Chrome flottant unifié = pastilles rondes « glass » (.hw-pv-btn, halo noir pour lisibilité sur photo claire) : fermer + plein écran en haut-droite, flèches nav (.is-lg), zoom = 3 pastilles empilées (zoom-in/out + RESET icône rotate-ccw, fini le boîtier et le « 100 % »). Vrai PLEIN ÉCRAN (API Fullscreen) masquant barre des tâches + chrome navigateur ; en plein écran, chrome minimal (tout masqué sauf les flèches, sortie via Échap). Pellicule = BANDE SÉPARÉE sous la photo (zones empilées façon Wikiloc, PAS un overlay → la photo ne passe jamais derrière les vignettes, robuste portrait ; viewer en flex-column) ; centrée si peu, pleine largeur + scroll si beaucoup (.is-overflow — anticipe la galerie de circuit) ; masquée en plein écran → la photo prend toute la hauteur. Icônes maximize/zoom-in/zoom-out ajoutées à lucide-icons. Image gardée en position:absolute+inset:0 (robuste vs le grid+max du handoff qui rognait les portraits). Tokens HW only.
- Viewer photo de consultation (handoff Claude Design) : un viewer unique plein écran immersif remplace le lightbox pauvre. Zoom/pan natif (molette, double-clic, paliers +/−/fit, pinch, double-tap, reset au changement de photo), pellicule de vignettes (active = --brand, PAS l'ambre proposé par CD), nav flèches/clavier/swipe mobile, état .is-single (1 photo) + loading. Clic sur le hero d'une fiche AVEC photo → ouvre le viewer (consultation) au lieu de la grille d'édition ; hero VIDE → grille (ajout) inchangé. collectPoiPhotoUrls (ui-details) agrège URLs publiées + blobs locaux (objectURL révoqués à la fermeture), publiées en tête. Double-listener hero supprimé (hydrateHeroFromBlobs). Image en position:absolute + inset:0 + object-fit:contain (boîte = stage exacte → letterbox tous ratios ; corrige le portrait qui débordait car height:100% en flux se résolvait contre 100vh, pas contre la stage). ui-photo-viewer.js réécrit (classes .hw-pv-*), modals.css enrichi (anciennes règles .hw-photo-viewer-* retirées), tokens HW only. Édition photos = grille « Gérer les photos », inchangée.
- Fix watermark admin sur l'import GPS : le chemin batch « Enregistrer » (compressFileToBlob, ui-photo-batch.js) ne tatouait PAS les photos, alors que la grille « Ajouter » (compressImage) le faisait → photos admin publiées SANS watermark sur le chemin de production PRINCIPAL. Fix : applyWatermark exporté depuis photo-service.js (source unique du dessin + texte) et appliqué dans compressFileToBlob si state.isAdmin. Taille/qualité du pipeline GPS inchangées (1600px/0.88). Pas de re-watermark rétroactif (photos de test à ré-uploader). +2 tests applyWatermark.
- Refonte Mon Espace V3 (handoff Claude Design) : 2 onglets (Sauvegarde + Corbeille) au lieu de 4 ; accent ambre→thème (--cta-bg, comme CC Admin). Onglet Sauvegarde allégé : Sauvegarder (choix explicite Légère/Complète + bouton ADAPTATIF « Télécharger » desktop / « Enregistrer·Partager » mobile, qui RESET le compteur backup-auto via resetBackupCounter), Restaurer (compact), statut backup auto en footnote 1 ligne. RETIRÉS : « Partager » (fusionné dans Sauvegarder), « Forcer un backup », « Sync à venir », onglet Sécurité, onglet Préférences/lieu de résidence (relocalisation au tri « Proximité » à venir — trou assumé, Stefan seul testeur, usage nov.). Onglet Corbeille gagne « Supprimer définitivement » (deleteCircuitById + modale de confirmation). CSS V3.1 intégré (tokens HW only, 1 seul #fff → var(--cta-fg)). Poids photos estimé sur la carte Complète.
- Fix sauvegarde/restauration des photos perso : les photos (Blobs du store poiPhotos) n'étaient JAMAIS incluses dans l'export — export ET import ne lisaient que state.userData, vidé de ses photos depuis la migration base64→Blob. Conséquences : la case « Inclure les photos » était inopérante (sauvegardes identiques avec/sans) et un user perdait ses photos à la restauration. Fix : l'export FULL encode les Blobs en base64 dans le bloc userData racine (collectPhotosAsBase64 + nouveau blobToBase64), SANS duplication dans le geojson ; restoreBackup redécode base64→Blob vers poiPhotos (base64ToBlob désormais exporté) et ne conserve que les URLs légères dans poiUserData. Runtime inchangé (Blob, compact) ; seul le fichier d'échange porte du base64 (+33%, attendu). Corrige les 2 chemins d'export (Mon Espace prepareExportData + menu/partage saveUserData). +5 tests export/import + 3 tests round-trip Blob↔base64.
- Fix BLOQUANT révélé au test de restauration : isValidBackup rejetait testedCircuits quand c'était un objet (n'acceptait qu'un tableau), or l'export ET le runtime produisent un objet { circuitId: bool } → AUCUNE restauration ne passait depuis l'ajout de testedCircuits à l'export (~#506). Désormais objet OU tableau accepté (rejet si null/primitive).
- Recherche POI : le focus va désormais sur l'onglet « Lieu » (fiche du POI cherché) au lieu de « Mes Circuits ». Le filtrage des circuits par ce POI est conservé (chip « Filtré par »). openDetailsPanel gagne un 3e param fromSearch (défaut false) qui pilote poiFilterFromSearch.
- Harmonisation champs contact + Facebook. L'éditeur HW « Éditer le lieu » (richEditor) gagne Téléphone + Horaires (parité avec le DM, ils manquaient côté HW). Nouveau champ public Facebook ajouté PARTOUT : éditeur HW (richEditor) + DM (index.html/main.js/storage.js) + affichage fiche (templates.js, ligne « Détails pratiques », icône f). Lien FB sécurisé : href http(s) only (bloque javascript:/data:) + target=_blank rel=noopener.
- Couverture de tests (#3, PR2) : loaders de config au boot (app-startup) — loadDestinationsConfig / loadOfficialCircuits / loadPoiCategoriesConfig testés sur valide + échec/fallback (404, réseau, JSON malformé, vide) → garde contre une dégradation SILENCIEUSE du boot. 10 tests, aucun changement de comportement (loaders déjà exportés).
- Couverture de tests (#3, PR1) : findFeaturesOnTrack (gpx.js) exporté + testé (tests/gpx_find_features.test.js — 8 cas : détection, seuil, threshold custom, ordre de passage, boucle/duplication, trace ouverte, vides). Aucun changement de comportement.
- Fix multi-destinations : le chemin d'upload d'un circuit (admin-control-ui) ne hardcode plus "djerba" — utilise state.currentMapId via GITHUB_PATHS.circuitFile(mapId, filename) (helper config.js ressuscité + corrigé, était du code mort). Évite qu'un circuit Hammamet/Agadir parte dans circuits/djerba/.
- Fix flake test backup_auto_local (non déterministe : passait isolé, échouait ~40% en suite complète). Cause racine : recordModification déclenche checkAndTriggerAutoBackup en arrière-plan non-awaité → ce trigger se terminait pendant le test suivant (appel saveUserData parasite), et le mutex _backupInProgress pouvait rester bloqué. Fix : hook test _flushAutoBackupForTests (drainage déterministe, remplace les setTimeout fragiles) + afterEach qui draine + _resetCacheForTests réinitialise mutex & pending. Zéro impact prod. Vérifié 20/20 runs verts.
- Fix circuit perso consulté : rendu épuré. Un perso enregistré simplement consulté rendait 4 éléments (poignée+numéro+nom+actions) dans la grille de consultation à 2 colonnes (data-mode="consult") → repli en 2×2, nom en mot-par-mot + boutons mal placés. isCreate (createStepElement) ne vaut désormais vrai que pour un brouillon ou state.editingMode (= complément exact de isConsult dans applyCircuitMode) → un perso consulté rend 2 éléments comme un officiel ; édition via "Modifier le circuit". Perso-only ; mobile non concerné.
- Destinations brouillon/publié : champ `status` sur destinations.json — Hammamet (draft) masqué du sélecteur + boot bloqué pour les non-admins (repli sur la dest publiée par défaut, anti-contournement ?map=hammamet). Admin voit les brouillons via un badge "Brouillon". Helper pur isDestinationPublished (défaut : status absent = brouillon).
- Point d'accès — PR C (peaufinage UX) : (1) retrait du toast ratable à l'entrée du mode pose (la barre persistante porte déjà la consigne ; le conseil « passe en OSM » était caduc — Voyager montre les routes, OSM grise au zoom). (2) Fix du grisé OSM/Voyager à fort zoom : Google Satellite monte à z20 mais OSM & Voyager plafonnaient à z19 → ces couches se grisaient dans le contrôle de calques dès z20 (impossible d'y revenir au zoom de pose). Correctif : maxNativeZoom=19 (tuiles agrandies au-delà) + maxZoom=20 sur planLayer et voyagerLayer (map.js) → couches sélectionnables sur toute la plage. Bénéfique à toute l'appli, pas que cette feature.
- Feature « point d'accès au tracé » — PR B (pose) : item kebab admin+desktop « Point d'accès au tracé » sur la fiche POI → mode pose carte (access-point-editor.js) : drapeau draggable (clic = place/déplace, glisser = ajuste) + ligne pointillée POI→drapeau, barre Enregistrer/Effacer/Annuler. Save via updatePoiData('accessPoint',[lon,lat]) → publié auto (geojson). Erase = null (prime sur le patrimoine) + strip au publish (admin-geojson) pour ne pas laisser de résidu. Chip admin « Point d'accès » sur la fiche + libellé propre dans le diff CC. Helper getAccessPoint extrait dans utils.js (réutilisé par gpx.js). Ne déplace JAMAIS le POI.
- Feature « point d'accès au tracé » — PR A (plomberie, sans effet visible) : generateGPXString (gpx.js) lit désormais, en Cas B (vol d'oiseau), un champ public OPTIONNEL accessPoint:[lon,lat] sur le POI et l'utilise comme ancre <trkpt> s'il est valide, sinon les coords réelles du POI (helper trackAnchorOf). Sert aux POI « hors voie » (bâtiment isolé) : l'admin posera (PR B) un drapeau sur la voie la plus proche → GPX Studio peut router jusque-là sans ignorer le POI. Le <wpt> (marqueur) reste TOUJOURS sur le vrai POI ; Cas A (realTrack) intouché ; aucun circuit existant affecté (champ absent → comportement identique).
- Fix export GPX « vol d'oiseau » : on retire le pré-snap routier OSRM (profil VOITURE) qui déplaçait les <trkpt> sur la route carrossable la plus proche. Conséquence du bug : pour un POI en retrait/sur un sentier, l'ancre du tracé atterrissait à côté du POI, et GPX Studio (qui s'ancre sur les <trkpt>) routait en IGNORANT le POI (POI laissé sur le côté). Désormais en Cas B (pas de realTrack), les <trkpt> = coordonnées RÉELLES des POIs → GPX Studio s'ancre pile dessus et fait lui-même le routage piéton. Helpers snapToNearestRoad/snapCircuitToRoads supprimés (+ param snappedCoords de generateGPXString). N'affecte que l'export à éditer ; les circuits publiés (realTrack, Cas A) sont intouchés.
- Fix RACINE des faux positifs circuit (remplace les rustines #624/#625 par la vraie cause) : l'index publié ré-ordonnait les poiIds par position sur le tracé GPS (generate-circuit-index.js, findPOIsOnTrack), alors qu'en local l'ordre est celui de SÉLECTION. Pour une BOUCLE, le POI de départ (présent au début ET à la fin du tracé) était trié en DERNIER → l'index ne matchait plus l'ordre local (faux positif « modifié » permanent dans le CC) ET la boucle s'affichait au mauvais départ côté user. Or ce tri-par-tracé était un VESTIGE de l'ancienne découverte par proximité ; depuis qu'on ne garde QUE les waypoints (« Disable proximity-based POI discovery »), ceux-ci ont déjà leur ordre. Fix : findPOIsOnTrack préserve l'ordre des waypoints (= sélection). Index djerba régénéré (seul le circuit en boucle change). Aucune modif du diff engine (le dédoublonnage #624 suffit). Corrige les 2 symptômes à la source.
- Fix (jumeau du #624) : circuits détectés à tort comme « modifiés » sur une simple différence de DESCRIPTION. Cause : la description ne fait pas l'aller-retour via le pipeline GPX → index. generate-circuit-index.js lit le <desc> des metadata GPX, hardcodé par generateGPXString à « Circuit généré par History Walk. » → l'index distant porte TOUJOURS cette constante, tandis qu'en local circuit-actions.js appose la signature « (Créé par History Walk) ». Les deux ne coïncident jamais → faux positif permanent. Fix : on retire la comparaison de description du diff circuit (admin-diff-engine.js). La description n'est pas un champ publiable côté circuit.
